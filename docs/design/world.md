# 11. マップ・イベント・NPC

関連: [`regions.md`](regions.md) / [`capture.md`](capture.md) / [`save-data.md`](save-data.md) / [`economy.md`](economy.md)

---

## 1. 設計方針

「原作のマップを再現」は最初の3つの希望の1つでありながら、**最も工数が重い**領域。
9地方で数百マップになる。

> **マップは「作る」ものではなく「流し込む」ものにする。**

つまり設計の目標は、良いマップを作ることではなく、
**マップを大量に流し込める仕組みと道具を用意すること**に置く。
[`README.md`](README.md) の中心原則（コンテンツ追加のコストをゼロに近づける）が、
最も強く問われる章になる。

## 2. マップデータ形式 ―【決定】Tiled を採用する

**決定事項: マップ制作には Tiled（無料のタイルマップエディタ）を使う。**
未決事項だった「Tiled の採用可否」をここで確定する。

### 理由

- タイルマップエディタの自作は数ヶ月規模の作業になる。**得るものがない**
- Tiled は JSON 出力に対応し、Phaser も公式にサポートしている
- **数百マップを作る以上、エディタの品質が生産性を直接決める**

### ただし Tiled 形式に依存しない

```
*.tmx / *.tmj （Tiled の出力・制作時の正）
      │
      │ tools/convert-map.ts   ビルド時に変換
      ▼
自作スキーマの JSON （ゲームが読むのはこちらだけ）
```

Tiled の JSON をそのままランタイムに渡すと、外部ツールの都合に縛られる。
**中間形式として扱い、ゲーム本体は自作スキーマだけを知る。**
将来エディタを乗り換えても、変換スクリプトを差し替えるだけで済む。

### 2.1 【訂正・v0.7】当面の原本はテキストのマップソースにする

Tiled 採用の決定は、上の「原本を差し替えても影響は変換スクリプトに閉じる」という
構えのおかげで、**痛みなく先送りできる**ことが実装してみて分かった。

**決定: v0.7 の原本は `packages/data/source/maps/*.map`（テキスト）とする。**

```
id: kanto-pallet-town
name: マサラタウン
region: kanto

legend:                 # 文字 → 地形。`!` 前置で通行不可
. normal
, grass
T !normal
D normal

grid:                   # 見たままの格子
TTTTT.TTTTTT
T..........T
...

warps:                  # 座標 種類 接続先マップ 座標 向き
5,0 step kanto-route-1 5,16 up
3,4 interact kanto-players-house-1f 3,6 up

objects:                # ID 座標 種別 [イベントID] [if:フラグ=真偽]
pallet-kid 8,6 npc:boy:static kanto.pallet.kid
pallet-oak-blocker 5,1 npc:oak:static kanto.pallet.oak-block if:kanto.pallet.got-starter=false
```

理由:

- **Tiled の価値はタイルを「塗る」ことにあるが、塗るタイルが1枚も存在しない**
  （[`../game-plan.md`](../game-plan.md) §10）。v0.7 で必要なのは
  通行可否・地形・接続・オブジェクトだけで、これは文字で書ける
- `maps-src/`（Tiled の原本）は**リポジトリに入れられない**（原作マップの写しのため）。
  一方この形式は数値と構造だけなので**コミットできる。**
  原本がコミットできないと、生成物と原本が別々の場所にある状態が常態化する
- 差分が読める。「どのマスの通行判定を変えたか」が git の diff にそのまま出る

**Tiled への移行は、`tools/convert-map.ts` に読み込み口を1つ足すだけ**で済む。
出力スキーマは変わらないので、ゲーム側は何も知らなくてよい ―― これが §2 の狙いだった。

## 3. マップの構造

```ts
type MapData = {
  id: MapId;                        // "kanto-pallet-town"
  region: RegionId;
  size: { width: number; height: number };   // タイル単位

  layers: {
    ground: TileId[];               // 地面
    decoration: TileId[];           // 上に乗る装飾
    overhead: TileId[];             // プレイヤーより手前に描く（木の上部など）
  };
  collision: boolean[];             // 通行可否
  terrain: TerrainId[];             // 草むら・水上・砂 等（エンカウント判定に使う §5）

  warps: Warp[];
  objects: MapObject[];
  encounters?: EncounterTableId[];  // capture.md §9 への参照。**地形が方式を決める**（§7.3）
  flyPoint?: { x: number; y: number; flag: FlagId };  // そらをとぶ の行き先（§7.2）
  onEnter?: EventId;                // マップに入った瞬間に走る（§7.2）
  bgm?: string;
};
```

- レイヤは3枚に固定する。可変にすると Tiled 側の運用が崩れる
- `collision` と `terrain` を**タイル画像から推測しない**。明示的な配列として持つ
  （見た目とルールを分離しておかないと、タイルを差し替えた瞬間に挙動が変わる）

### マップ接続

```ts
type Warp = {
  at: { x: number; y: number };
  to: { map: MapId; x: number; y: number; facing: Direction };
  trigger: "step" | "interact";     // 踏む / 調べる（ドアなど）
};
```

**検証項目**: 全 `warp.to.map` が存在し、座標がマップ範囲内で、通行可能タイルであること。
数百マップを手で繋ぐ以上、接続ミスは必ず起きる。CIで潰す（[`data-schema.md`](data-schema.md) §6）。

## 4. オブジェクトと NPC

```ts
type MapObject = {
  id: string;
  at: { x: number; y: number };
  kind:
    | { type: "npc"; sprite: SpriteId; movement: "static" | "wander" | "route" }
    | { type: "trainer"; trainer: TrainerId; sight: number; direction: Direction }
    | { type: "item"; item: ItemId; hidden: boolean }
    | { type: "sign" }
    | { type: "obstacle"; clearedBy: FieldAbilityId }    // §7
    | { type: "boulder"; pushedBy: FieldAbilityId }     // §9.12（v1.1-f）
    | { type: "switch" };                               // §9.12（床。通行を妨げない）
  event?: EventScript;              // §6
  condition?: Condition;            // このフラグが立っていれば出現／消滅
};
```

- **トレーナーの視線**（`sight`）は、向いている方向に N マス。原作の挙動（v0.12 で実装・§9.5）
- **`movement` は `static` だけ実装する。**【決定・v0.12】NPC が歩くと経路探索の道具が壊れる（§9.5）
- `condition` により、イベント進行でNPCが現れたり消えたりする
- 撃破済みトレーナーは `flags` で管理し、再戦は原作準拠で行わない（施設で戦えるため）

## 5. エンカウント判定

1歩ごとに判定する（原作準拠）。

```
歩いたタイルの terrain がエンカウント対象か
        │ Yes
        ▼
エンカウント率で抽選 → 成立したら EncounterTable から抽選
```

- エンカウント率は `terrain` ごとに定義（草むら・洞窟・水上で異なる）
- 抽選は [`capture.md`](capture.md) §9 の `EncounterTable` を参照
- **連続で出続ける／全く出ない、の救済を入れる**（直前のエンカウントから最低歩数、
  一定歩数を超えたら確率上昇）。原作より緩めてよい ―― 9地方を周回する本作では、
  理不尽な連続エンカウントは摩擦として重すぎる

### 5.1 実測（v0.7）― 「救済」はひとつのものではなかった

草むらだけを 15 万歩ぶん歩かせて測った（基本遭遇率は 12% に設定）。

| 設定 | 遭遇回数 | 平均歩数 | 最小 | 中央 | 最大 | 実効遭遇率 |
| --- | --- | --- | --- | --- | --- | --- |
| **採用した設定**（猶予5歩＋pity） | 11,399 | 13.2 | 6 | 11 | **37** | **7.6%** |
| 猶予なし（pity のみ） | 18,099 | 8.3 | 1 | 6 | 36 | 12.1% |
| 救済ぜんぶ無し（原作風） | 17,660 | 8.5 | 1 | 6 | **79** | 11.8% |

分かったこと:

1. **`rateByTerrain` は実効遭遇率ではない。** 猶予歩数5があるだけで 12% → 7.6% に落ちる。
   猶予は「連続で出続ける」を止めるつもりの仕掛けだったが、
   実際には**遭遇の総量そのものを4割減らしている。** 設定値を体感値だと思ってはいけない
2. **pity は平均をほとんど動かさない**（8.5 → 8.3）。効くのは分布の裾で、
   最悪ケースを **79歩 → 37歩** に半減させる
3. したがって **2つを「救済」とひとくくりにして増減させてはいけない。**
   猶予は総量を削る調整、pity は裾を切る調整で、効く場所が違う

この表は `packages/core/src/world/movement.ts` の `ENCOUNTER` の直上に写してある。
実効値を固定するテストも置いた（`world.test.ts`）ので、
猶予歩数を触ると実効率が動いたことが検出される。

## 6. イベントシステム ―【最重要】

[`save-data.md`](save-data.md) の `RegionProgress.flags` は**器だけが存在していた。**
それを操作する仕組みがここになる。

### 表現 ―【決定】JSON のコマンド列にする

| 案 | 判断 |
| --- | --- |
| TypeScript でハードコード | ❌ 地方追加のたびにコードが増える。本設計の原則に反する |
| 独自スクリプト言語 | ❌ パーサとエディタ支援の自作コストが見合わない |
| **JSON のコマンド列** | ✅ **採用。** 技効果・ボール補正・ルールセットと同じデータ駆動 |

```ts
type EventScript = EventCommand[];

type EventCommand =
  | { kind: "message"; speaker?: string; text: string }
  | { kind: "choice"; prompt: string; options: { text: string; then: EventScript }[] }
  | { kind: "if"; cond: Condition; then: EventScript; else?: EventScript }
  | { kind: "setFlag"; flag: FlagId; value: boolean }
  | { kind: "battle"; trainer: TrainerId; onWin?: EventId; onLose?: EventId }  // 参照のみ
  | { kind: "giveItem"; item: ItemId; count: number }
  | { kind: "givePokemon"; species: SpeciesId; level: number }
  | { kind: "takeMoney" | "giveMoney"; amount: number }
  | { kind: "healParty" }
  | { kind: "warp"; to: MapId; x: number; y: number }
  | { kind: "moveObject"; object: string; path: Direction[] }
  | { kind: "faceObject"; object: string; direction: Direction }
  | { kind: "wait"; frames: number }
  | { kind: "playSe" | "playBgm"; id: string }
  | { kind: "shop"; inventory: ShopId }          // economy.md §5
  | { kind: "openBox" | "openDex" };

type Condition =
  | { kind: "flag"; flag: FlagId; value: boolean }
  | { kind: "badges"; op: ">=" ; count: number }
  | { kind: "hasItem"; item: ItemId }
  | { kind: "hasSpecies"; species: SpeciesId }
  | { kind: "and" | "or"; of: Condition[] };
```

### インタプリタ

- `packages/core` に置く。**描画に依存しない**
- `message` や `wait` のような**演出を伴うコマンドは、実行を中断して UI に制御を返す**
  （[`ui-flow.md`](ui-flow.md) §4 のバトルと同じ考え方 ―― core は時間を持たない）
- イベントは**原子的に完了する**ものとして扱い、実行の途中状態はセーブしない。
  中断が必要な長大イベントは、フラグで区切って複数イベントに分割する

### `battle` コマンドだけは原子性を保てない

バトルは数分かかる。**「イベントは原子的」という前提と正面から衝突する。**
ブラウザを閉じられたら、イベントの途中でセーブが存在しない状態になる。

**決定: `battle` コマンドはイベントを2つに分割する境界として扱う。**

```
イベント前半（会話 → battle 開始）
      │  ここでフラグを立て、バトルを開始する。イベントはここで終了
      ▼
バトル（この間の中断は「バトルからの離脱」として扱う）
      │
      ▼
イベント後半（フラグと勝敗を条件に、別イベントとして起動）
```

- `battle` コマンドは `EventScript` の**最後のコマンドでなければならない**（検証項目）
- `onWin` / `onLose` は**別のイベントIDへの参照**にする。インラインのスクリプトにしない
- バトル中に中断された場合は、次回起動時に**バトル直前の状態から再開**する
  （トレーナー戦をやり直す。進行は巻き戻らない）

インラインの `onWin` を許すと「バトルを含むイベントの途中状態」が発生し、
セーブできない領域がゲーム内に生まれてしまう。**型のレベルで塞ぐ。**

### フラグIDは静的データ側で宣言する

```ts
// data/regions/kanto/flags.json
["kanto.oak.received-starter", "kanto.gym1.cleared", "kanto.rocket.hideout-open", ...]
```

- **宣言のないフラグを `setFlag` / `Condition` が使っていたら検証エラー**にする
- タイプミスで永久に立たないフラグは、発生してから原因を突き止めるのが極めて困難。
  [`data-schema.md`](data-schema.md) §6 の「調整項目」だったが、**宣言必須で確定する**
- 命名は `<地方>.<系統>.<内容>` のドット区切り

## 7. フィールド技 ―【決定】秘伝技を廃止する

原作では いあいぎり・なみのり 等の秘伝技を覚えたポケモンが進行に必要になる。

**決定: 秘伝技を廃止し、進行能力は「道具または進行フラグ」で解放する。**

```ts
type FieldAbilityId =
  | "cut" | "surf" | "strength" | "rockSmash" | "fly"
  | "oldRod" | "goodRod" | "superRod" | "itemfinder"   // v1.1-c
  | "waterfall" | "flash";                              // v1.2-a
```

- 障害物は `{ type: "obstacle"; clearedBy: FieldAbilityId }` として置く
- **押せる岩は別の kind**（`boulder`・v1.1-f）。どちらも「能力で対処する通れないもの」だが、
  障害物は*消える*・岩は*動く*ので、持つ状態が違う（§9.12）
- 対応する能力は、バッジ取得や道具入手で**プレイヤー自身が獲得する**（手持ちに依存しない）

### 理由

- 秘伝要員がパーティ枠を圧迫するのは、**9地方を周回する本作では特に苦痛**になる
- 「技を覚えさせる／忘れさせる」という独立したサブシステムが丸ごと不要になる
- 第7世代以降の原作も実質この方向（ライドポケモン等）

[`capture.md`](capture.md) §8、[`progression.md`](progression.md) の
「周回の摩擦を減らす」方針と一貫している。

### 7.2 訂正 ―― 束ねてあった決定を、片方だけ解く（v1.2-a）

§7 の決定は「秘伝技を廃止する」と1文で書いてあるが、**中身は2つあった**:

1. **秘伝マシンを道具にしない**（覚えさせる手段を作らない）
2. **フィールドで使う条件を、手持ちではなくプレイヤー自身が持つ**

②は本作の骨格で、周回の摩擦を減らすという方針そのもの ―― 変えない。
①はその**帰結として書かれただけ**で、②を守ったまま外せる。

**外した。** 秘伝マシンは道具になり、覚えさせて戦闘で使える。
フィールドでは今までどおり、覚えているポケモンが1匹も居なくても使える。

> **1文にまとめた決定は、あとで片方だけ間違っていたときに分けられない。**
> 分けられたのは、②が「なぜそうするか」を別に書いてあったから。

外せたのは6本。`hm02 fly` は そらをとぶ が溜め技で機構が無く、
`hm08 dive` は**潜る水面が1マスも無い** ―― どちらも `machines.tsv` の
`skip` 欄に理由が書いてある（「入れていない」と「入れ忘れ」を分ける）。

### 7.3 フラッシュ と たきのぼり ―― 原作の7つがそろう（v1.2-a）

| | 形 | `core` に足したもの |
| --- | --- | --- |
| **たきのぼり** | `TERRAINS` に `waterfall`、能力の効果は **`walk` を使い回す** | **1語だけ** |
| **フラッシュ** | `FieldRule.dark`（見えるマスの半径）。読むのは描画だけ | 効果の種類 `light` 1つ |

**たきのぼり に新しい仕掛けは要らなかった。** `FieldAbility` の `walk` は
「その地形の上を移動できる」をすでに言えて、`walkableTerrains` が通行判定を組み立てる
―― なみのりと同じ形に乗るので、`core` に増えたのは地形名1語。

**フラッシュは「明るくする」以外に何もしない。** 暗さは壁ではなく**幕**で、
持っていなくても歩けるし戦える（原作もそう）。だから `core` は暗さを知らず、
`FieldEffect` に足した `light` は**何もしない**と書いてある関数になる ――
既定の `clear` に流すと、フラッシュが障害物をどける能力に見えてしまう。
**黙って既定に流すのと、理由つきで何もしないのは別。**

#### 「その場所だけの規則」は1種類ではなかった

暗さの置き場所は `MapData.rules`（サファリのために作った1欄）。
そこへ入れたとき、`FieldRule` の `steps` / `expire` が必須で、
**暗い洞窟に `steps: 0` と書いて「入った瞬間に追い出される」ものを作った。**

必須だったのは規則が1種類しか無かったからで、性質ではない。両方を任意にし、
**片方だけ書いてあるのは書き忘れとして落とす**（数えるのに出口が無い／
出口があるのに数えない）。ついでに「歩数も暗さも戦えなさも無い規則」も落とす
―― 指しても何も変わらない規則は、置いた本人にしか意図が分からない。

#### 検証 #121 ―― 地形を足しただけでは関門にならない

たきつぼ に滝の帯を引いたが、**帯の端に岸を1マス残せば歩いて回り込める。**
そのとき滝は飾りになるのに、#55 も #80 も「行けてしまう」を正しいとしか言わない
（v1.1-e で かいりき の岩に起きたのと同じ形）。

**その能力を外して、届かなくなるマスが1つ以上あるか**を見る。
数えるのは**その地形そのもの以外**のマス ―― 滝のマスは滝の能力でしか乗れないのが
当たり前で、それを数えると必ず通る（空振りの検査になる）。
帯に1マス岸を戻して、実際に落ちることを確かめた。

### 7.1 実装（v0.12-d）

5つとも実装した。**ゲームのコードに「いあいぎり」という文字列は1つも無い。**
能力の一覧・解放条件・文面は `packages/data/field-abilities.json` にある。

| 決めたこと | そうした理由 |
| --- | --- |
| 解放条件は `Condition` を使い回す | 分岐・NPCの出現条件と同じ型。「解放条件」専用の型を作ると書き方が2つに割れる |
| 条件は **フラグ ∧ バッジ** の2段 | 教われば覚える／バッジが licence。原作の「バッジが無いと使えない」をデータだけで表せる |
| `WorldState.abilities` は**派生値**。保存しない | 保存すると「フラグは消えたのに なみのり だけ使える」状態を作れてしまう |
| どけた障害物（`cleared`）も**保存しない** | 原作と同じで出入りすれば戻る。セーブに「世界のどこを壊したか」が溜まらない |
| どけた印は `マップID:オブジェクトID` | オブジェクトIDはマップの中でしか一意でない。名前だけだと別の町の岩まで消える |
| 水は**通行不可のまま置き、能力で例外にする** | 逆にすると、能力の実装を消した瞬間に海の上を歩けるマップができる。既定は塞がっている側へ倒す |

消える処理は `visibleObjects()` の1箇所に入れた。
当たり判定・描画・トレーナーの視線がそこを通るので、**3つが勝手に揃う。**

> **秘伝マシンは道具にもしない**（v1.1-b）。わざマシンを取り込むとき、
> 公式データには HM01〜HM08 も同じ表に入っている。技として実装済みのもの
> （いあいぎり・なみのり・かいりき・たきのぼり）もあるが、**「教える道具」を作ると
> 廃止した機構が裏口から戻ってくる。** `machines.tsv` に理由つきで8件落としてある。

### 7.3 何をする能力か（v1.1-c）

v0.12-d の5つは**どれも「障害物をどける」**だった。だから `FieldAbility` は
解放条件しか持っていなくてよかった ―― 釣りと探知が入って初めて、
**「何をする能力か」という欄が要る**ことが分かる。

```ts
type FieldEffect =
  | { kind: "clear"; then?: { method } }   // どける（省略時これ。既存5件は無変更）
  | { kind: "walk"; terrain }              // その地形の上を移動できる
  | { kind: "fish"; method }               // 水面を調べて釣る
  | { kind: "reveal" }                     // 隠れているものを見つける
  | { kind: "travel" };                    // 行った町へ飛ぶ
```

**省略したら `clear`。** 既存の いあいぎり・かいりき はデータを1文字も変えていない。

| 決めたこと | そうした理由 |
| --- | --- |
| `fieldActions` は**1問だけ**答える（向いているマスに対して何をするか） | 「移動できるか」と「調べたら何が起きるか」を1つの関数にすると、どちらでもない `walk` が両方に嘘の答えを返す |
| `walk` と `travel` は `null` を返す（効く場所が違う） | **黙って何も書かないのと、理由つきで何もしないのは別**（`inert` な特性と同じ運用） |
| さおは**良いものが先に当たる**（選ばせない） | 原作に無い操作。選択肢が1つ増えるたびに歩みが遅くなる |
| いわくだき の野生は `clear.then` の1行 | 「どける」と「出る」を1つの能力に持たせたので、他の能力にも `then` を足すだけで付く |
| 隠しアイテムは `visibleObjects()` で消す | **v0.12-d の障害物と同じ1箇所。** 描画・踏みつけ・視線が勝手に揃う |

#### `canSwimTo` に "water" と書いてあった

v0.12-d の実装は `terrain === "water" && abilities.includes("surf")` ―― **`core` が
特定の地形と特定の能力の名前を知っていた。** 地形と能力の対応は
`field-abilities.json` にしかないので、そちらから導く形に直した。

`WorldState.walkable`（`abilities` と同じ派生値・保存しない）に入れる。
足したとき**片方だけ入れ直す場所が4箇所あって、実際に
「なみのり は使えるのに水に入れない」世界ができた** ――
`syncAbilities()` に2行まとめて、呼ぶ側が選べないようにした。

#### 固定シンボル ―― `EventCommand` に1つ

三鳥・ミュウツー・カビゴンは「その場で始まる野生戦」。
`battle`（トレーナー戦）に相乗りさせず `wildBattle` を足した ――
**勝っても逃げても「もう そこには居ない」**という終わり方が違うため。

フラグは**戦う前に立てる。** 結果で分けると、逃げたときだけシンボルが残る。
v0.7 から宣言だけされていた `EncounterTable.method: "static"` は、
結局この形では要らなかった（表ではなくイベントが持つ方が素直だった）。

### 7.2 そらをとぶ

行き先は**マップ自身が名乗る**（`MapData.flyPoint`）。
「行ける町の一覧」を別の表に持つと、マップを消したときにそこだけ残る。

`flyPoint.flag` は「一度でも来たか」の記録で、`onEnter`（マップに入った瞬間に走る
イベント）で立てる約束にしてある。**約束を破ると検証 #82 が落ちる** ――
行き先として宣言したのに一生選べないマップは、遊んで気づくには遠すぎる。

`onEnter` はこの版で足した唯一のイベント口で、四天王の「戻れない部屋」もここで塞ぐ。

### 7.3 なみのり と出現テーブル

水の上を歩けるようにしたら、**草むらのポケモンが海から出てきた。**
`MapData.encounters` が1つしか持てず、`EncounterTable.method` を誰も見ていなかった。

- `encounters` を配列にし、**地形が方式を決める**（`METHOD_BY_TERRAIN`）
- 表が無い地形では抽選そのものをしない（乱数を空回しすると再生が合わなくなる）

## 8. マップ作成のワークフロー

数百マップを作る以上、**道具立てがそのまま生産性になる。**

1. Tiled でマップを描く（タイルセットは地方間で共有する）
2. `tools/convert-map.ts` で自作スキーマへ変換
3. `tools/validate.ts` で検証
   - warp の接続先が存在し、通行可能タイルか
   - `encounters` の参照先が存在するか
   - `trainer` / `item` / `flag` の参照が存在するか
   - 到達不能な領域がないか（**プレイヤーが入れない/出られない区画の検出**）
4. 地方の全マップが揃ったら [`capture.md`](capture.md) §7 の到達可能性検証を回す

**タイルセットは地方間で共有する。** カントーとジョウトは特に共有率が高く、
[`regions.md`](regions.md) §2 で実装順を隣接地方にした理由がここにある。

## 9. 実装の段階

| 版 | 内容 |
| --- | --- |
| **v0.7** | 1つの町 + 1本の道路。移動・衝突・warp・エンカウント・最小のイベント |
| v0.8 | ポケモンセンター／ボックス端末のイベント |
| v0.9 | ショップのイベント |
| v0.10 | カントー全域。ジム・ダンジョン・フィールド技による障害物 |
| v1.0 | 四天王・殿堂入りのイベント演出 |

**v0.7 の完了条件**: 町と道路を往復でき、草むらで野生と遭遇し、
NPCと会話してフラグが立ち、そのフラグで別のNPCの反応が変わること。
イベントシステム全体の疎通確認をここで済ませる。

### 9.1 v0.7 の結果

完了条件は満たした。マサラタウン・主人公の家・オーキド研究所・1番道路の4枚で、
家 → 町 → 研究所（最初の1匹）→ ライバル戦 → 町 → 1番道路 → 野生戦 → 逃走 まで通る。
同じ道行きを2通りで検証している ―― `core` だけで通す単体テストと、
ブラウザを実際に動かす `tools/playthrough.mjs`。

**フラグによる分岐は2つの形で効く**ことを確認した:

- `Condition` を持つオブジェクトは**存在ごと消える**。
  最初の1匹を受け取るまで、町の北口はオーキド本人が塞いでいる
- イベント内の `if` は**同じNPCの台詞を変える**

#### 実装が設計を訂正したところ

**1. 「マップを1枚足すコスト」は思ったより座標にある。**
コードは1行も増えなかった（狙いどおり）が、**手で書いた座標は必ず間違える。**
このバージョンで検証が捕まえたのは、warp の接続ミスではなく
「机に囲まれて一生話しかけられない NPC」と「四方を塞がれた1マス」だった。
そこで検証項目 #55（到達不能な区画）に加えて **#56（話しかけられないオブジェクト）**
を足した。**歩けるマスが全部繋がっていることと、置いたものが起動できることは別。**

**2. `battle` を「末尾のみ」に縛った判断は正しかったが、縛り方が足りなかった。**
§6 では「`EventScript` の最後のコマンド」と書いていたが、
`if` や `choice` の枝の中に `battle` がある場合を規定していなかった。
実装では**「自分が属する列の末尾であり、かつ祖先も全て末尾である」**まで縛った。
ライバル戦は「選んだ1匹に応じて相手が変わる」ため `if` の3分岐の底に `battle` があり、
この規定が無いと最初の実データで破れていた。

**3. `givePokemon` の技指定が UI まで届いていなかった。**
コマンドは `moves?` を持っていたのに、`EventEffect` に載せ忘れていた。
型は通り、テストも通り、**ブラウザで動かしてヒトカゲが「ひのこ」1つで戦っていた**
ことで初めて分かった。`tools/playthrough.mjs` を書いた理由がこれ。

**4. 段差は「地形」で表現できた。**
一方通行の向きを地形IDに持たせる（`ledge-down` 等）案もあったが、
南向き以外は9地方を通してごく少数なので、**1方向に固定して定数1つで表した。**
必要になったら地形を増やす。

#### 【訂正・v0.8】ドアは「調べる」ではなく「踏む」

v0.7 では家と研究所のドアを `interact`（調べる）warp にしていた。
**その結果、建物に入れなかった。**

ドアのマスは歩けるので、近づいて「上」を押すと **調べる前にドアの上へ乗ってしまう。**
乗ってしまえば目の前は壁なので、そこで「しらべる」を押しても何も起きない。
原作でもドアは踏んで入るもので、`interact` は看板や机に使うものだった。

**検証項目 #60 を足した ―― 「調べる warp が歩けるマスにあってはならない。」**
歩けるマスにある `interact` warp は、**構造的に絶対に起動できない**。
到達不能な床（#55）や話しかけられないNPC（#56）と同じ種類の誤りで、
やはり「置いたのに起動しない」形をしている。

#### 【追加・v0.8】押しっぱなしで歩く

1入力1歩にしていたため、スマホでは**指で何度も叩く**操作になっていた。
さらに歩行アニメ中の入力を捨てていたので、連打しても進まなかった。

- 「今どちらを押しているか」を状態として持ち、**離すまで歩き続ける**
- 向き直りの直後に、同じ方向へ**もう一度だけ試す** ――
  進める先なら1タップで動き、壁や看板なら向き直りだけで止まる。
  「壁際で向きを変えられる」という向き直りの目的は損なわれない
- 十字キーから「けってい」を分けた。十字の中央に置くと、
  歩いているつもりで話しかけてしまう

**歩かなかったとき（向き直り・壁）にも待ち時間を入れた。**
これが無いと、押しっぱなしの繰り返しが `await` を挟まずに回り続け、
**画面が固まって指を離すことすらできなくなる。** 実装中に一度そうなった。

#### v0.7 では**やっていない**こと

- **世界の状態を保存していない。** フラグ・所持金・手持ちはリロードで消える。
  `SaveData` のスキーマ変更（v2 → v3）を伴うので v0.9 の IndexedDB 移行とまとめる
- **HP がマップに持ち越されない。** 手持ちはまだ `PartySpec`（設計図）で、
  戦うたびに満タンから始まる。`healParty` も演出だけ。**v0.8 の最優先はこれ**
  （[`../game-plan.md`](../game-plan.md) §8.3 論点3の結果）
- **トレーナーの視線**（`sight`）は型にあるだけで判定していない。v0.10
- NPC の `movement`（`wander` / `route`）も同様に未実装

### 9.2 v0.9 の結果 ― 町を1つ足すのに、コードは1行も要らなかった

トキワシティ（町・ポケモンセンター・フレンドリィショップ・民家の4枚）を足した。
**変更したのは `.map` ファイル4枚・`events.json`・`flags.json`・`shops.json` だけ。**
`packages/game` も `packages/core` も1行も触っていない。

これは v0.7 の「エンジンとデータを分ける」判断が実際に効いた最初の証拠になる。
ただし、無条件ではない ―― 検証が**9件のエラーで止めた**。

| 検証が止めたもの | 内容 |
| --- | --- |
| #47 warp の接続 | 建物の入口が、接続先の warp マスの上を指していた（入った瞬間に押し戻される無限往復）×3 |
| #48 オブジェクト | カウンターや机の上に NPC を置いていた ×5 |
| #55 到達可能性 | 北の出口を作ったが、警備員で塞いだので誰も立てないマスが1つ残った |

どれも**そのまま出せば「入れない建物」「話しかけられない店員」になる。**
v0.7 の収穫（#55・#56）と同じ形の誤りを、町を1つ足しただけで3種類やり直している。
**人はマップを置くたびに同じ間違いをする。** 検証に落としておく価値はここにある。

### 9.3 `WorldState` と `PlayerState` の二重の持ち主を解消した

`core` が読み書きする `WorldState`（`flags` / `badges` / `money` / `bag` / `partySpecies`）と、
UI が持つ `PlayerState` が、**同じものを別々に持っていた。**
v0.9 で `player` を唯一の持ち主にした（[`save-data.md`](save-data.md) §9.7）。

繋いだ瞬間に、**同じ道具を2回もらっていた**ことが分かった ――
`core` の `giveItem` ハンドラと UI の `gotItem` 効果が両方 +1 していた。
入れ物が別だったので、合計だけが静かに倍になっていた。

### 9.4 台本は座標を数えるのをやめた

`tools/playthrough.mjs` は v0.7 から「押した回数」でマップを歩いていたが、
**町を1つ足しただけで全部通らなくなった。**
今は `maps.json` を読んで経路探索する（段差の一方通行・踏む warp・オブジェクトの
通行判定まで含む）。目的地はマップをまたいで指定できる。

副産物として、**負けて家に飛ばされても台本が続く**ようになった。
座標を数える台本は、想定していない場所に飛んだ時点で意味を失っていた。

### 9.5 v0.12 の結果 ― 視線・ジム・バッジ

§4 に型だけあった `sight` を実装した。規則は原作のまま
（向いている方向に N マス／壁と他のオブジェクトで切れる）で、
1つだけ足したのが **近い方を優先する**という決まり。
視線が重なるマスで遠い方が先に来ると、すり抜けて奥から呼ばれたように見える。

**視線は野生より先に見る。** 草むらを横切ってトレーナーの前に出たとき、
野生が割り込むと「見つかったのに何も起きない」状態になる。

#### 視線は画面に描く

原作は視線を描かない。だが**本作のトレーナーはただの四角**なので、
どちらを向いているか分からないまま見つかるのは理不尽でしかない。
向いている側に印を置き、届く範囲を薄く塗る。
**判定と同じ規則で切る**（`spotterAt` と同じく壁とオブジェクトで途切れる）
―― 見えているものと当たり判定がずれると、そちらの方が理不尽になる。

#### 「！」は押さなくても消える

見つかったのはこちらの操作ではない。
「押さないと進まない」を挟むと、理不尽さの上に手間が乗る。

#### NPC は動かさない ―【決定】

`movement` は型に `wander` / `route` があるが、**`static` 以外は実装しない。**

理由は経路探索。`playthrough.mjs` も `shots.mjs` も
「オブジェクトは動かない」前提でマップを歩いている。
NPC が歩くと**この2つの道具が同時に壊れる**わりに、得られるのは町の賑わいだけ。

賑わいが欲しくなったら「**向きだけ変える**」で足す。マスを動かさない限り、
経路探索は成り立ち続ける。検証 #79 が `static` 以外を止める。

#### バッジは増分ではなく到達点

`giveBadge` が受け取るのは「これで何個目になるか」。
同じイベントを2回踏んでも増えず、ジムの順番を入れ替えても数が狂わない。
バッジは進行の目盛りなので、**何個持っているかだけが意味を持つ**。

#### 台本が「視線」で歩けなくなった

視線バトルは**会話から始まる**（向こうから話しかけてくる）。
`goToMap` は会話を捌かずに次のキーを押していたので、
以降の入力が全部そちらに吸われ、**歩いていないのに歩いたことになっていた。**
会話を消してから進む形に直した。

同じところでもう1つ。ジムを通すために手持ちを Lv50 まで上げたのに、
**ヒトカゲの「ひっかく」と「ひのこ」ではイワークを削り切れず、全滅した**
（ぼうぎょ160・ほのお半減）。レベルだけでは相性を越えられないことが、
台本の側で先に出た形になる ―― ゲーム内の子どもが言っているとおりだった。

### 9.6 v0.12-b の結果 ― 町2つとジム2つを、コード0行で足せた

§9.2 で「町を1つ足すのにコードは1行も要らなかった」を確かめた。
v0.12-b はその**8倍の規模**（マップ14枚・トレーナー11人・イベント45件）で
同じことを試した結果になる。

| 足したもの | コード |
| --- | --- |
| 3番道路・おつきみやま・4〜6番道路 | 0行 |
| ハナダシティとジム2（カスミ）・ポケセン・ショップ・民家 | 0行 |
| クチバシティとジム3（マチス）・ポケセン・ショップ | 0行 |
| **バッジで品揃えが変わるショップ** | **0行** |

**バッジ連動のショップにコードが要らなかった**のは、
イベントの条件に `badges` が v0.7 から入っていたため。
`if` で `shop` の `inventory` を選ぶだけで済む。

```
いらっしゃいませ!
if badges >= 4 → shop kanto-mart-3
else if badges >= 2 → shop kanto-mart-2
else → shop kanto-mart-1
```

足したコードは**検証1つだけ**（下記 #80）。

#### 訂正: 到達可能性を1枚の中でしか見ていなかった

検証 #55 は**マップ1枚の中**の孤立区画を探していた。
おつきみやまを書いたとき7行目の壁が右まで届いていて**南半分が丸ごと切れていた**が、
そこは「到達できないマス」ではなく「行き止まりだが繋がっている区画」なので素通りした。

気づいたのは、台本が「経路なし」と言ったとき。

**訂正: 地方の入口から、その地方の全マップへ歩いて行けることを見る（#80）。**
踏む warp を辿り、段差の一方通行も条件つきオブジェクトも同じ規則で扱う。
足したあと**わざと壁を戻して、実際に止まることを確かめた。**

#### 台本の穴が2つ出た

1. **条件つきのオブジェクトを素通りできる前提だった。**
   経路探索は「進行で消えるもの」を壁として数えない。だが実際には塞いでいるので、
   おつみみやまのやまおとこに**永久にぶつかり続けていた**。
   ぶつかったら話しかける（人がやることと同じ）ようにした。
2. **`drain()` が店を閉じていた。** 品揃えは `#menu` ではなく
   会話枠の選択肢として出る。`drain()` は選択肢の**いちばん下**を押すので、
   開いた瞬間に「やめる」を選んでいた。

どちらも「道具が世界の作りに追いついていない」形で、
**世界を広げるたびに道具の前提が1つ壊れる**、という v0.9 からの傾向がそのまま続いている。

### 9.7 v0.12-c ― ジムの順番も、条件だけで縛れた

シオン・ヤマブキ・タマムシ・セキチクとジム4〜6を足した。マップは 33 → 53枚。
**ここもコードは1行も要らなかった**（検証も足していない）。

原作のバッジ順は エリカ(4) → キョウ(5) → ナツメ(6) だが、
地理的にはヤマブキが手前にある。原作は「飲み物を欲しがる警備員」で塞いでいた。

本作は**バッジの数で塞ぐ**。

```
けいび:
  if badges >= 5 → 「どうぞ おとおりください」＋ 通す
  else           → 「バッジ 5つの ちょうせんしゃしか あいません」
```

`badges` はイベントの条件に v0.7 から入っている。
**進行の順番はデータの問題であって、コードの問題ではない** ―― これで3回目の確認になる
（v0.9 の町追加、v0.12-b のショップ、今回のジム順）。

#### ポケモンタワーとサファリゾーンは「置くだけ」にした

どちらも原作では専用の機構がある（ゴーストの正体・ボールを投げる別ルール）。
**機構が無いまま形だけ作らない**という v0.4 からの方針に従い、
タワーは1階だけ・サファリは看板だけにしてある。
「まだ しめとる」と言う管理人が居るのは、**無いことを黙って隠さない**ため。

### 9.8 v0.12-d ― フィールド技。ここで初めてコードが要った

v0.9・v0.12-b・v0.12-c と「町を足すのにコードは0行」が3回続いたが、
**フィールド技は0行では足せなかった。** 足した機構は3つだけ:

| 足したもの | なぜコードが要ったか |
| --- | --- |
| `fieldAbilitiesFor` / `obstacleKey` / `canClear` | 「持っている能力」という概念そのものが無かった |
| `interact` の `obstacle` 分岐 | 調べた先が**イベントでも warp でもない**3つめの答えになった |
| `MapData.onEnter` | 「マップに入った瞬間」という口が1つも無かった |

逆に**データだけで済んだ**もの:
5つの能力の解放条件・解放してくれる NPC・障害物の設置・そらをとぶ の行き先9つ。
能力を1つ増やすときに触るのは `FIELD_ABILITIES` の1行と JSON だけになっている。

#### 3つの前提が同時に壊れた

1. **`EncounterTable.method` を誰も見ていなかった。**
   水の上を歩けるようにした瞬間、草むらのポケモンが海から出てきた。
   `encounters` を配列にし、地形が方式を決める形へ直した（§7.3）。
2. **到達可能性が障害物を壁として数えていた。**
   #55 も #80 も「置いた岩の先」を到達不能と報告した。
   どちらも**地形として繋がっているか**を見る場所なので、障害物と水は通れる扱いにした。
   「今の進行度で行けるか」は #84（その技が世界のどこかで手に入るか）が別に見る。
3. **`wild-usable` が Lv5 固定で測っていた。**
   なみのり の表を Lv15〜 で作ったのに「Lv5 で技が無い」と言われた。
   実際に出るレベルで測り直した ―― **測る対象が違えば、通っても落ちても意味が無い**
   （v0.12 のライバル戦と同じ誤り）。

#### 置き場所は、条件が正しくても間違える

セキチクの おおきな いわ を最初 (10,8) に置いた。
条件も種別も正しかったが、そこは**ポケモンセンターの入口へ登るマス**で、
街の施設が丸ごと閉じた。#80 が「ポケセンへ歩いて行けない」と言って止めた。

v0.12-c のヤマブキの警備員（条件は正しいのに扉を塞げていなかった）と同じ形で、
**向きだけが逆**になっている。どちらも検証が拾ったので、置き場所は目視で守らない。

### 9.9 v0.12-e ― カントーが輪になった

グレンじま・トキワジム・19〜21番水道を足して**ジム8つが揃った**。マップは 53 → 61枚。
**コードは1行も足していない**（検証を1つ足しただけ）。

いちばん大きいのは形が変わったこと。

| | v0.12-d まで | v0.12-e |
| --- | --- | --- |
| カントーのマップ | 49枚 / 隣接ペア 48 | 57枚 / 隣接ペア 57 |
| 木か | **ちょうど木**（辺 = 頂点 − 1） | **閉路が1つ** |

21番水道が グレンじま と マサラタウン を結んだので、
**グレンへ南から入って北から出られる。** ここまでのカントーは、どの道路も
ちょうど2か所を繋ぐだけの一直線だった。

閉路を作るのに要ったのは `.map` を3枚書くことだけで、
`warp` で部屋を任意に繋ぐ作り（初代〜BW と同じ）が最初から閉路を許していた。
検証 #80 も BFS なので、そのまま通っている。

グレンじまへは**なみのり が無いと行けない。** これは文章ではなくテストで縛ってある
（能力別に踏破可能なマップを塗って、`kanto-cinnabar-island` が入らないことを見る）。

#### 訂正: 建物を出ると町の反対側に立っていた

`#85` を足したその場で、**7か所が落ちた。**

```
kanto-fuchsia-city (10,7) → kanto-fuchsia-pokecenter から戻ると (3,4)＝10マス離れた場所に出る
```

v0.12-c の4つの町（タマムシ・ヤマブキ・シオン・セキチク）で、ポケモンセンターや
ショップを出ると**町の反対側に飛ばされていた。** 原本を写して座標だけ直し忘れた形。

見逃した理由がはっきりしている ――
**「歩けないマス」でも「繋がっていない」でもない**ので、#47 も #55 も #80 も通る。
遊べば1秒で分かるのに、機械の目には正常に見える種類の壊れ方だった。

> **#85: 入って出たら、入口の前に立っている。**
> 出入口が1つしかない建物についてだけ見る（複数ある建物は対がひとつに決まらない）。

足した直後に**わざと1件戻して、実際に止まることを確かめた**（#80 と同じ手順）。

### 9.10 v0.12-f ― 四天王。カントーが1本で終わるようになった

22・23番道路、チャンピオンロード、セキエイこうげん、四天王4人＋チャンピオンを足した。
マップは 61 → 71枚。**ここもゲームのコードは0行**（検証を1つ足しただけ）。

#### 「戻れない部屋」は、機構ではなくデータで作れた

v0.12 の計画では、ここが**「v0.12 で初めて本物のコードが要る場所」**だと見ていた。
実際に書いてみると要らなかった。

| やりたかったこと | どう作ったか |
| --- | --- |
| 入ったら戻れない | **戻る warp を置かない。** それだけ |
| 次の扉は勝つまで開かない | 扉のマスに、条件つきのオブジェクトを1つ置く |
| 負けたら追い出される | 既存の全滅処理がそのまま働く（復活地点へ戻る） |

「戻れない」は**足す機構ではなく、置かない選択**だった。
`onEnter`（v0.12-d）を使うつもりでいたが、使わずに済んでいる ―― 
**先に用意した口が、結局要らなかった**という記録として残す。

#### かいりき の見せ場

チャンピオンロードの (4,2) に かいりき の岩を置いた。
北の出口へ続く道はここ1本なので、**岩をどけないと抜けられない。**

v0.12-d では かいりき は道具1つのための寄り道だったが、
ここで初めて進行そのものを止めている。
「抜けられないこと」も「どけると抜けられること」もテストで縛ってある。

#### 検証 #86 ― 出入口を、消えないもので塞いでいないか

扉のマスにオブジェクトを置く形にしたので、**条件を書き忘れると
その出入口は永久に死ぬ。** 見た目は普通の扉で、踏んでも何も起きない。

> **#86: 踏む warp のマスに、条件の無いオブジェクトが乗っていないこと。**

条件つきなら正しい使い方（勝つまで開かない扉）なので、そこだけを見分ける。
足したあと**わざと条件を外して、実際に止まることを確かめた。**

#### 台本が四天王に勝てなかった ―― 戦い方の問題だった

四天王を通そうとして、**ワタルに3連敗**した。手持ちは Lv100 が6匹。
負ける道理が無いので調べたところ、台本の戦い方そのものが原因だった。

| 直したこと | なぜ効いたか |
| --- | --- |
| 技を**順番に回す**（先頭から押していた） | ワタルは5匹中3匹が ひこう。**じしん を延々と0ダメージで撃っていた** |
| **とくしゅ技で揃える**（物理で殴っていた） | リザードンは こうげき 84 / とくこう 109。半分損をしていた |
| 手持ちを6匹に・ラムのみを持たせる | キクコの さいみんじゅつ + あやしいひかり はレベル差では解けない |

いちばん効いたのは**技を回す**ことだった。
「先頭の技を押し続ける」は、相性が0倍の相手に当たった瞬間に無限ループになる。

**これはゲームの欠陥ではなく、台本の欠陥。** 人なら1ターンで気づく。
ただ「レベルを上げれば通る」で押し切らずに済んだのは、
**負けた理由を数えたから**で、レベルを上げるほうを先にやっていたら
ずっと原因に気づかないままだった。

## 9.8 「隣とは何か」を1箇所にした（v1.1-a）

v1.0 まで、**同じ判定が3箇所に書かれていた。**

| どこ | 何を見ていたか |
| --- | --- |
| `tools/validate.ts` | 地方の連結性（#55）と、マップ1枚の塗りつぶし（#55・#56） |
| `tools/playthrough.mjs` | 台本の経路探索 |
| `tools/shots.mjs` | 撮影の経路探索 |

3つとも「段差は南向きにだけ2マス飛ぶ」「踏む warp は同じ1歩の中で移る」
「水は なみのり で越えられる」を**それぞれ書いていた**。
書いてある内容が同じうちは害が出ない ―― 出るのは規則が増えたときで、
**氷の床（v1.1-f）はこの3つを同時に壊す。**

そこで `core` に置き、道具はそれを呼ぶだけにした。

```ts
neighborsOf(map, world, x, y, options, mapById): Neighbor[]
canEnter(map, world, x, y, options): boolean
```

**能力は `WorldState` で渡す。** 台本は「なみのり を覚えた」「この岩をどけた」を
そのまま `abilities` / `cleared` に入れる ―― ゲーム本体と同じ形なので、
**経路探索とゲームが食い違いようがない。**

違うのは「見立て」だけで、options はその2つに絞ってある。

| 見立て | 誰が使うか | なぜ |
| --- | --- | --- |
| `ignoreConditional` | 検証・両台本 | 進行で消える門番を壁として数えると、開通済みの道まで閉じる |
| `ignoreObstacles` | 検証・撮影 | どければ通れる岩を壁にすると、その先が全部「到達不能」になる |
| `followWarps: false` | 塗りつぶし（#55） | 1枚の中の「入れるが出られない区画」を探している |

### 一本化して初めて食い違いが見えた

`shots.mjs` は**コメントに「障害物は壁のまま」と書いてあり、コードはそうなっていなかった。**
どちらが正しいか決めずに置き換えたら、撮影が2か所で `△`（狙いと違う場所を撮った）を出した
―― チャンピオンロードの岩の先が全部撮れなくなっていた。

正しいのはコードの方だった（撮影用のセーブは能力を全部持っている）。
**3箇所に散っている間は、コメントと実装がずれても誰も気づかない。**

### 世界を広げるたびに、道具の前提が1つ壊れる（4件目）

| 版 | 広げたもの | 壊れた前提 |
| --- | --- | --- |
| v0.12-b | 洞窟 | 到達可能性をマップ1枚の中でしか見ていなかった |
| v0.12-d | 障害物・なみのり | 経路探索が「今できること」を知らなかった |
| v0.12-f | 戻れない部屋 | 撮影が水を通れず、別の町を撮っていた |
| **v1.1-a** | **海の出現表** | **`spot()` が「フィールドは必ず出ている」と思っていた** |
| **v1.1-b** | **バッグから使う道具** | **`#open-bag` を「開く」だと思っていた（実際は開閉の切り替え）** |
| **v1.1-c** | **道をふさぐ固定シンボル** | **撮影用セーブがカビゴンをどけていなかった（8枚が16番道路で止まった）** |

クチバの海には v1.0 まで出現表が無かった。取り込みで15枚ぶんの surf 表が付いた瞬間、
**なみのり の1歩の直後にバトルが割り込む**ようになり、`#field-canvas` を
30秒待って台本が落ちた ―― **一度も通っていなかった道**が、データが増えた日に通った。

直し方はいつも同じ形になる。**「たまたま起きなかったこと」を前提にしない。**
歩く関数は、現在地を見に行く前に「今バトルなら片付ける」を挟む。

カビゴンの件は**気づくのが1版おくれた。** v1.1-c では通し確認だけを回して
撮影を回さなかったからで、通しは たまたま カビゴンを起こす区間が
セキチクより先にあった ―― **順番が違えばそこで止まっていた。**

> **道具は2本とも回す。** 片方だけ緑なのは「壊れていない」の証拠にならない。

**ただし `spot()` の中でやってはいけない。** 最初そう直したら、
野生戦の回数を数える区間が「0回」になった ―― 数えたい相手を、
現在地を見るついでに黙って倒していた。**片付けるのは歩く関数の中だけ。**

### 台本の誤報は、ゲームの不具合に見える（v1.1-b）

わざマシンを「おぼえない」で断ったとき、台本は
**「やめたのに道具が 0こ になった」**と報告した。ゲームの側の取り返しのつかない
不具合に見えるが、実際には**1こ のまま残っていた。**

`#open-bag` は開く/閉じるの**切り替え**で、道具を使ったあとパネルは開いたまま
描き直される。そこをもう一度押した台本は**バッグを閉じ**、空のパネルを読んで
「0こ」と数えていた。

**確かめ方はいつも同じ。** 短い台本を1本書いて、その操作だけをブラウザでやってみる
―― 30分の通し確認の中で疑うより速く、しかも切り分けが1回で済む。
以後、台本の側では「押す」ではなく**「開いていることにする」**と書く。

### 台本を強くした変更が、別の台本の前提を壊す（v1.1-b）

v1.1-a で `goToMap` に「割り込んだバトルを片付ける」を足した。**それ自体は正しい。**
だが全滅の検査は「HP を1にする → 草むらへ歩く → 負ける」の順に書かれていて、
**歩いている途中で全滅し、ポケモンセンターで回復してしまう**ようになった。
草むらに着いた時点では満タンで、確かめたい状態が消えている。

直し方は**順番の入れ替えだけ**（草むらに着いてから弱くする）。
仕組みではなく段取りの問題なので、コードは1行も増えない。

> **「道具を強くしたら、その道具を使う側の前提を1つ壊す」**
> ―― §9.8 の表と同じ形が、台本どうしの間でも起きる。

## 9.9 公式データを2箇所から採ると、版がずれる（v1.1-b）

わざマシンの番号と技の対応は **FRLG（第3世代）** から取り込んでいる。
どの種がマシンで何を覚えるかの互換表は **第9〜7世代** から採っている
（`fetch-official.ts` が learnset の `'M'` を選る）。

**この2つは揃っていない。**

> でんげきは（tm34）は FRLG ではマシンだが、いま**誰もマシンでは覚えない。**

道具にすると「バッグに入っているが、どの手持ちに使っても断られるマシン」ができる。
書いた瞬間の検証 #95 がこれを捕まえた ―― **1本だけなので、手で見ていたら気づかない。**

対処は2つに分けた。

1. `fetch-machines.ts` が「互換表に持つ種がいない」を判定して落とす。
   **理由つきで `machines.tsv` に残る**ので、いつか互換表の版を変えたときに戻ってくる
2. マチスの贈り物だけ 10まんボルト に替えた（原作は でんげきは）。
   でんげきは 自体は技として実装済みで、レベルでは覚えられる ――
   **消えたのは「マシンとして配る」経路だけ**

**じてんしゃも同じ用心をしている。** 速くするのは見た目だけで、
**1入力＝1マスは変えない** ―― 2マス進む実装にすると `playthrough.mjs` と
`shots.mjs` が同時に、しかも黙ってずれる（§9.8 の3件目と同じ壊れ方）。
`core` は時間を持たないので、影響は `field.ts` の定数1つに閉じる。

## 9.10 構造の嘘 ―― ヤマブキが2叉路だった（v1.1-d）

v1.0 までのカントーには、**通っているのに間違っている**接続が1つあった。

| | v1.0 まで | 原作 |
| --- | --- | --- |
| 5番道路 | ハナダ → **6番道路へ直通** | ハナダ → ヤマブキ**北門** |
| 6番道路 | 5番道路 → クチバ | ヤマブキ**南門** → クチバ |
| ちかつうろ | 無い | 5番⇄6番の**任意の近道** |
| ヤマブキの出入口 | 2つ（西・東） | **4つ** |

**近道のほうが本道になっていた。** 地図としては繋がっているので、
検証も台本も何も言わない ―― 見つかるのは原作の地図と見比べたときだけ。

### けんもんじょ は「2つ warp を持つ小マップ」

ゲート建物はポケモンセンターと同じ形で、**新しい機構は1つも要らなかった。**
4棟足して、5〜8番道路の warp を繋ぎ替えただけ。

ちかつうろ も同じ ―― 入口の家2軒＋通路1枚で、
**ヤマブキを通らない道が「近道として」存在する**状態に戻った。

### 地理を直すと、難易度が変わる

ヤマブキを4叉路に戻すと、**ハナダからクチバまでの道が2倍近くなった**
（5番 → 北門 → 町 → 南門 → 6番）。v1.0 までは5番から6番へ直通していた。

結果、通し確認がマチス（ジム3）に**負けるようになった。**
着くころには手持ちが削れていて、しかも回復に寄っていなかった。

**気づけたのはバッジの数を見ていたから。**
その前の行は `マチス戦の 決着: 決着してマップに戻った` で緑だった ――
「決着した」は**勝敗を含まない。** 勝敗を見ていたのは次の行だけ。

> **測る対象が1つずれていると、緑の行が嘘をつく。**
> v1.1-a の「エンカウントの比」と同じ形の間違いが、台本の側にもあった。

直し方はジムの前にポケモンセンターへ寄る ―― **人がやることと同じ。**
（`challengeGym` は使わない。あれは先頭で Lv100 にするので、
序盤の実戦がまるごと消える。）

**これは不具合ではなく、原作に寄せた結果として正しい。**
道が長くなれば消耗する。原作でもクチバの前にポケセンがある。

### 検証が7種類の間違いを1回で捕まえた

7枚のマップを足して繋ぎ替えたとき、最初の `npm run validate` で出たエラー:

| 出たもの | 何を間違えたか |
| --- | --- |
| 接続先が踏む warp の上（2件） | 扉のマスに着地させていた（無限往復になる） |
| 接続先が通行不可（2件） | 建物の壁に着地させていた |
| 調べる warp が歩けるマスにある（3件） | 階段を `interact` にしたが、他の扉は全部 `step` |
| 往復すると4〜6マス離れる（3件） | ジム・ポケセン・ショップの**戻り先が古い座標のまま** |
| 地方の連結性（1件） | `interact` の warp は `neighborsOf` が辿らない |

**最後の1件が設計の答えになった。** 階段だけ `interact` にする理由が無い ――
他の扉は全部 `step` なので、揃えたら連結性の問題も消えた。
**「例外を作らない」が、そのまま検査を通る道だった。**

## 9.11 東の腕とダンジョンの階層（v1.1-e）

シオンタウンは行き止まりではなくなり、**同じ迷路の使い回し**も解消した。

| | v1.0 まで | v1.1-e |
| --- | --- | --- |
| シオンの出入口 | 2方向 | **3方向**（北=10番・西=8番・南=12番） |
| おつきみやま | 1枚 | **1F / B1F / B2F** |
| チャンピオンロード | 1枚 | **1F / 2F / 3F** |
| 2つの迷路の一致 | **13行中12行** | **0行** |
| 地図の独立した輪 | 1本 | **5本** |

出現表は**階ごとに引く。** まとめて引くと1階に地下の種が混ざる。
イワヤマトンネル B1F に `rock-smash` の表が出て、
v1.1-c で機構だけ入れて毎回警告していたものがここで意味を持った。

### 迷路には迂回路ができる

チャンピオンロードの かいりき の岩は、最初**通らなくても階段に着けた。**
1階の縦道と横道が輪になっていて、岩はその片側にしか無かった。

検証は何も言わない ―― **「行けてしまう」は繋がっていることなので正しい。**
気づいたのは、短い台本で「岩をどける前に2階へ行けるか」を測ったとき。

> **関門は「置いた」ではなく「そこしか通れない」で初めて関門になる。**
> 迷路を1枚描いたら、関門の手前と先で経路探索を1回ずつ回す。

関門の先を撮るときも同じことが効いた。撮影は「チャンピオンロード2階」を
**1階とは別の場所として**撮ろうとして届かなかった ―― どけた岩は
`world.cleared` にしか残らず、**マップを出入りすれば元に戻る**（§7.1 の決定）。

直し方は並び順を変えるだけ。1階を撮って岩をどけた**すぐ後**に2階を撮る。

> **保存しないと決めたものは、順番に依存する。**
> それ自体は正しい決定で、依存するのは道具の側。

### ポケモンタワーは v1.1-g に送った

機構は v1.1-c で揃っている（ゴースト＝同じマスに条件で入れ替わる2つの
オブジェクト＋ `hasItem`。コードは0行）。足りないのは**シルフスコープの
入手元**だけで、あれはロケット団アジトにある。

先に階だけ作ると**登れない7階**ができる ――
機構が無いまま形だけ作らないのと同じ理由で、**入手元が無いまま鍵だけ作らない。**

## 9.12 押せる岩とスイッチ（v1.1-f）

**この版で初めて世界が動く。** ここまでの「変わるもの」は全部
*消える*ものだった ―― どけた岩（`cleared`）・見つけた隠しアイテム・
条件で入れ替わるオブジェクト。消えるものは**どこへ消えたかを持たなくてよい**。
押せる岩は行き先を持つので、初めて「動いた位置」という状態が要る。

| | 保存するか | 持ち方 |
| --- | --- | --- |
| どけた岩・木 | しない | `WorldState.cleared`（id の集合） |
| **押した岩** | **しない** | **`WorldState.moved`（id → 座標）** |
| スイッチが立てたフラグ | **する** | `flags`（既存の機構） |

### 出入りすると岩は戻るが、扉は開いたまま ―【決定】

`moved` を保存しない理由は `cleared` と同じで、加えて**詰み防止**でもある ――
保存すると「岩を隅に押し込んで二度と解けないダンジョン」がセーブの中に固定される。

一方フラグは保存されるので、**一度開けたシャッターは二度と閉じない。**
原作より緩いが、`cleared` のときと同じ向きに倒した ――
**解いた手応えを取り上げない。** 岩が戻るのは「もう一度解ける」であって
「もう一度解かされる」ではない。

### 新しい `Condition` は作らなかった

「岩がスイッチに乗っているか」は条件になりかけたが、作らなかった。
岩の位置は**保存しない派生値**なので、`Condition` に載せると
保存するもの / しないものの境界が濁る。代わりに:

```
岩が乗る → スイッチの event が走る → setFlag → 以降は既存のフラグ機構
```

**保存しない状態が、保存する状態に変わる瞬間を1箇所に閉じ込めた。**
`Condition` の種類は v0.7 から5つのまま増えなかった ――
**v1.1-h で6つ目（`money`）を足すまでは**（§9.15）。

### 差し替えは `visibleObjects()` の1箇所

岩の座標を差し替えるのは `visibleObjects()` だけ。この関数はすでに
「消えるもの」を落とす唯一の場所だったので、当たり判定・描画・視線・調べる が
**勝手に揃った**。実際、押した岩の当たり判定を別に書く必要は無かった。

> **世界の「今の見え方」を作る関数が1つあれば、動くものを足すのは1行で済む。**

### 2階の岩も飾りだった ―― §9.11 の見落とし

v1.1-e で1階の輪を閉じたとき、**2階を見ていなかった。** 2階も部屋が輪に
なっていて、置いてあった かいりき の岩を通らずに階段へ行けた。

> §9.11 の教訓（関門は「そこしか通れない」で初めて関門になる）は正しかったが、
> **直したのは見つけた1枚だけだった。** 同じ形の間違いは同じ版の中に複数ある。
> 教訓を得た版の中で、同じ構造の場所を全部数えないといけない。

今は2階・3階とも「岩をスイッチに乗せる」以外の道が無い。
台本が関門の手前で1回・先で1回、経路探索を回して確かめている ――
実測で 173項目 → **179項目**、所要 12.1分（1歩 95ms）。

### 氷の床は入れていない ―【決定・v1.3以降】

計画では v1.1-f を「氷の床**と**押せる岩」としていたが、氷は入れなかった。
**カントーに氷の床が1枚も無い**からで、これは調べて分かったこと ――
ふたごじま は水流と穴、チャンピオンロードは岩とスイッチで、
氷の床が出てくるのは ジョウトの こおりのぬけみち 以降。

機構だけ先に入れると、**使うマップが1枚も無い機構**ができる。
それは v0.4 から守っている「機構が無いまま形だけ作らない」の裏返しで、
v1.1-a で消費者のいないデータ（ソーナンス）を足して検証に落とされたのと同じ形。

滑走そのものの設計（`StepOutcome` に `slid`・1回の呼び出しで全部返す・
`neighborsOf` の隣が着地マスになる）は計画のまま有効なので、
**ジョウトを作る版でそのまま実装する。**

## 9.13 ジムの仕掛け3つ（v1.1-g）

原作の仕掛けを14個並べたとき、**6個は既存の型で書ける**と見積もった。
そのうち3個をまとめて実装した ―― どれも「新しい概念」を1つも足していない。

| 仕掛け | どう書いたか | ゲームのコード |
| --- | --- | --- |
| **見えない壁**（セキチクジム） | 凡例に `I !normal` を1つ。描画では床として扱う | **2箇所**（下記） |
| **クイズ扉**（グレンジム） | `choice` + `if` + `setFlag` + 条件つきオブジェクト | **0行** |
| **テレポート床**（ヤマブキジム） | `Warp.to.map` に**自分自身のマップID** | **0行** |

### 訂正: 見えない壁は「`TILE_HINT` に1行」では済まなかった

計画は「凡例に文字を1つ、`art/tiles.ts` の `TILE_HINT` に1行」と書いていた。
色は確かに1行で合った。だが**オートタイルの縁は「隣が同じ文字か」で決まる**ので、
色だけ合わせると見えない壁の周りにだけ縁が出て、**床の上に格子が浮かんだ。**

直したのは `TILE_ALIAS`（見た目だけ別の文字として扱う表）を1つ足して、
`kindAt` をそこに通すこと ―― 2箇所・実質5行。

> **見た目の規則が2つに分かれているとき、片方だけ直すと「ほとんど正しい」絵ができる。**
> 色と隣接は別の判断で、別の場所にある。

### テレポート床は「型では前からできた」書き方だった

`Warp.to.map` に自分自身を書くのは v0.7 から型として通っていたが、
**前例が1件も無かった。** 9つの小部屋を壁で完全に切り、床を踏む以外に
行き来できなくしてある。正しい順は S → E → F → C → G。

`neighborsOf` は踏む warp を「同じ1歩の中で」辿るので、
**経路探索も台本も何も知らずに解ける** ―― 台本に足したのは0行。

### 検証 #107・#108 ―― 飾りと、永久に閉じた扉

| # | 見ているもの |
| --- | --- |
| 107 | **テレポート床が飾りでない** ―― 床を1枚も踏まずに全マスへ歩けてしまうなら、それは仕掛けではなく模様 |
| 108 | **条件で道を塞ぐものは、その条件を満たせる** ―― フラグを立てるイベントが無ければ、扉は永久に閉じたまま |

#107 は v1.1-e（迷路の輪）・v1.1-f（2階の岩）と**同じ形の間違い**を、
今度は起きる前に捕まえるために書いた。
#108 は**フィールド技の解放条件には同じ検査があったのに、
道を塞ぐオブジェクトには無かった** ―― 同じ不変条件が、置き場所によって
片方だけ守られていた。

### 台本は「間違える」ほうも試す

クイズ扉は、正解だけ試すと**どちらを選んでも開く扉**を通してしまう。
台本はまず わざと間違えて扉が開かないことを見てから、3問とも正解する。
`drain()`（最後の選択肢を押す）とは別に、
**文字で選ぶ `choose()`** を足したのはこのためだけ。

## 9.14 ダンジョンと伝説 ―― 図鑑が動いた（v1.1-g-2）

マップ 91 → **102枚**。足したのは11枚:

| 足したもの | 何のため |
| --- | --- |
| 17・18番（サイクリングロード） | 16番がセキチクに直結していた ―― **原作より1本短かった** |
| 24・25番＋マサキの家 | ハナダの北の腕。原作の4叉路のうち北だけ無かった |
| むじんはつでんしょ | **サンダー** |
| ふたごじま（2階） | **フリーザー** |
| ポケモンやしき（2階） | ミュウツーの生まれた場所。日記でミュウに繋がる |
| ハナダのどうくつ | **ミュウツー**。殿堂入りするまで入口にけいびが立つ |

**入手経路のある種が 92 → 101 に動いた。** ここまでの v1.1-d/-e/-g-1 は
「地理は直ったが図鑑は動かない」版が続いていた ―― 道路を足しても、
そこに出る種はもうどこかで出ていたから。

> **図鑑を動かすのは道路ではなくダンジョン。**
> 原作の固有種は、たいてい行き止まりの奥に置いてある。

### 17・18・24・25番は、種を1つも増やさなかった

道路4本ぶんの出現表を取り込んで、増えた種は**0**。
公式データを見ると、そこに出るのは スピアロー・オニドリル・ドードー・
コラッタ・ラッタ・キャタピー・ビードル・ポッポ・ナゾノクサ ―― 全部よそにも出る。

**これは失敗ではなく、測り方が効いた例。** 「マップを足したから前進した」
と思い込まずに済んだのは、入手経路を数える物差しがあったから。

### ファイヤーの居場所を変えた ―【決定】

FRLG のファイヤーは**1のしま（カントーの外）**に居る。その島はまだ作っていない。
赤緑の配置（チャンピオンロード）に置いた ――
**居場所が無い種を、居ないままにはしない。**
三鳥のうち2羽だけ取れる状態のほうが、原作への忠実さより悪い。

### 借りが2件目 ―― テレポート

24・25番には ケーシィ が出る。だが ケーシィ は Lv16 まで **テレポート**
しか覚えず、あれは「野生戦から逃げる」効果で機構が無い。
メタモン（へんしん）と同じ形なので、同じように**理由つきで野生から外した。**

除いても今より減らない（ケーシィは今もトレーナーの手持ちにしか居ない）が、
**テレポートを1つ入れれば ケーシィ・ユンゲラー・フーディン の3種が開く。**

### 道具の前提が3回壊れた ―― 2つは世界のせい、1つは自分のせい

ダンジョンを11枚足したあと、**撮影が3回止まった。**

| 見えていたもの | 本当の原因 | 種類 |
| --- | --- | --- |
| `#field-canvas` が無い（落ちる） | **PP切れで自滅していた** | 世界を広げたら壊れた |
| ヤマブキジムだけ △ | **警備員のフラグを立て忘れ** | 世界を広げたら壊れた |
| △ が 1件 → **6件** | **推測で経路実行を作り替えた** | **直したら壊れた** |

1つめ。撮影用の手持ちは素の PP（15/20/25）だった。道中の野生が増えたぶん
60戦ほどで尽き、わるあがき で自滅して全滅する ―― 見えていたのは
「復活の演出のあいだに撮ろうとして落ちた」といういちばん外側だけだった。

> **撮る道具は、負けないようにするのが正しい。** 勝敗は撮る対象ではない。

2つめは v0.12-e から在った穴。トキワジムの `gym-open` は撮影セーブで
立てていたのに、**ヤマブキのぶんを忘れていた。**

3つめが本題。報告はこうだった:

```
△ ヤマブキジム … ねらい kanto-saffron-gym 6,7 / いま kanto-saffron-city 4,4
```

ジムの扉は町の (5,3)、警備員は (5,4)。**(4,4) は扉の1マス手前**で、
座標は最初から答えを持っていた。それを読まずに
「テレポート床（自分自身への warp）を経路実行が見逃している」と推測し、
座標での突き合わせに作り替えて、**関係ない5件を壊した。**

しかもその作り替えは、歩行アニメ（130ms／自転車で62ms）の途中で
座標を読むので毎歩ずれたことになり、全経路を引き直し続けていた。
元の版が「マップ id だけ」で足りていたのは、**id はアニメ中も変わらない**から。

> **推測で直す前に、止まった座標が何を意味するかを見る。**
> **精度を上げるなら、比べるタイミングの前提も一緒に見直す。**

なお「経路は warp を辿って引いてある」ので、同じマップの中で飛ぶ
テレポート床は**最初から正しく歩けていた** ―― 直す必要のないものを直した。

## 9.15 サファリゾーン ―― その場所だけの規則（v1.1-h）

**`Ruleset` には寄せなかった。** あれは施設の編成と連戦の型
（`teamSource` / `levelMode` / `streakCap`）で、歩数もエサも戦闘不可も入る場所が無い。
寄せると**4施設すべてが無視する任意欄が8個増える** ―― 「1件足すコストを
ゼロに近づける」の正反対になる。

代わりに足したのは**1欄だけ**:

```
MapData.rules?: FieldRuleId      .map のヘッダに「rules: safari」
packages/data/field-rules.json   歩数・追い出し先・戦えるか・投げられるボール
```

`encounters:` / `onEnter:` と同じ形なので、**2つ目のサファリは JSON 1件**で増える。

### 歩数は保存しない

`cleared`（どけた岩）・`moved`（押した岩）と同じ派生値にした。
規則のある**区画**へ入るたびに数え直す ―― セーブに「あと何歩」が溜まると、
**歩数を変えた日に古いセーブが壊れる。** それは規則ではなく事故。

### 数え直す単位はマップではなく区画

最初は「規則のあるマップへ入るたびに数え直す」と書いて実装した。
`arrive()` が `cleared` と `moved` を捨てるのと同じ場所に1行足すだけで済み、
筋が通っているように見えた。**通っていなかった。**

サファリは4枚のマップで1つの場所なので、中央から西へ1歩入っただけで
500歩に戻る。**歩数制限が制限でなくなる** ―― 尽きる前に隣へ移ればいいだけになり、
「あるきすぎると おわりです」は言うだけの台詞になる。

v1.1-e で覚えた **「関門は『置いた』ではなく『そこしか通れない』で初めて関門になる」**
と同じ形だった。歩数制限も、**尽きて初めて**制限になる。

直し方は、いま効いている規則の id を持ち、**規則が変わったときだけ**数え直す:

```ts
if (rule === null) { ruleActive = null; stepsLeft = null; }
else if (rule.id !== ruleActive) { ruleActive = rule.id; stepsLeft = rule.steps; }
```

`cleared` / `moved` は**マップを跨いだら戻る**のが正しく、歩数は**区画を跨いだら
戻らない**のが正しい。同じ「保存しない派生値」でも、**捨てる単位は同じではない。**
一緒に捨てられる場所に並んでいるという理由だけで、同じ寿命だと決めていた。

### 置いた場所が寿命を決める ―― 同じ間違いを、違う軸でもう一度

区画で数えるように直したあとも、歩数はまだ戻っていた。今度は**メニュー**で。

`#open-settings` は `field?.stop()` のあと `playField()` を呼び直す
（`main.ts` の `showField`）。歩数を `playField` の閉包に持たせていたので、
**セーブ画面を開いて閉じるだけで500歩に戻る** ――
歩数制限を無効にする手順が、遊ぶ側の手元にあった。

持ち主を `player` に移した。`toSave()` が書き出す欄に入れていないので
**セーブには載らない**（派生値のまま）が、寿命はマップ画面より長い。

| | 1度目 | 2度目 |
| --- | --- | --- |
| 戻る操作 | 隣のエリアへ1歩 | メニューを開いて閉じる |
| 原因 | `cleared`・`moved` と同じ場所に並べた | `playField` の閉包に置いた |
| 直し方 | 数え直す単位を**区画**にした | 持ち主を **`player`** にした |

> **保存しないと決めても、それだけでは寿命は決まらない。**
> **どこに置いたかが決める。**

`cleared` / `moved` にも同じことは起きる（メニューを開くと切った木が生え直る）が、
あちらは**遊ぶ側が損をする向き**なので穴ではない。歩数だけが得をする向きだった ――
**同じ挙動でも、どちらに転ぶかで「仕様」か「穴」かが変わる。**

### のこり歩数は画面に出す

出していなかったせいで、**減っていないのか数え直されたのかを画面から言えなかった。**
500歩あるいたつもりで追い出されない回、原因を切り分ける手がかりが1つも無い。

診断のために足した表示だが、**遊ぶ側にも最初から要るものだった** ――
あと何歩か分からないまま追い出されるのは、規則ではなく理不尽。原作も出している。

### `Condition` に6つ目を足した ―― `money`

設計時に**「新しい `Condition` は1つも作らない」**と結論していた。さお・探知・
シルフスコープ・自転車・サファリ入場・クイズ扉・ゲートの通行止めを全部並べて、
`hasItem` / `flag` / `badges` で表せることを確かめた ―― **入場料だけ数え漏らした。**

「サファリ入場は `hasItem` で足りる」と書いたとき見ていたのは*入れるかどうか*で、
**払えるかどうか**ではなかった。`takeMoney` は足りなければ有るぶんだけ取って
黙って通す（`Math.min`）ので、**払えないことを「払えない」と言える場所が
払う前しか無い。** 0円でサファリに入れていた。

```ts
| { kind: "money"; op: ">="; amount: number }   // v1.1-h
```

`badges` と同じ形。`world.money` は既にあったので、評価は1行。

> **「全部数えた」の確からしさは、数えた軸の数で決まる。**
> 7つの仕掛けを漏れなく並べても、「持っているか」だけ見て「払えるか」を
> 見ていなければ、漏れは仕掛けの数では見つからない。

### エサ・イシは道具にしない

バッグに入れるとサファリの外へ持ち出せてしまい、
「その場所だけの規則」という前提が消える（`MapData.rules` の意味が無くなる）。
`Action` に `{ kind: "safari"; throw: "bait" | "rock" }` を1つ足し、
効き方は `capture.ts` に閉じた ―― ボールの補正レジストリの隣。

| | 捕まえやすさ | 逃げやすさ |
| --- | --- | --- |
| イシ | **上がる** | 上がる |
| エサ | 下がる | **下がる** |

**2つが逆に動くのが唯一の駆け引き**で、技が使えないぶんここが「削る」の代わりになる。
原作は「怒り」と「食事」を別々の状態として持つが、**1本の軸にまとめた** ――
2つ持つと「怒りながら食べている」が表現でき、その意味を決めないといけなくなる。

### `BattleState.safari` は真偽値にしない

`{ rocks, baits } | null` にした。**「段階を持っているか」がそのまま
「サファリか」の判定**になるので、`isSafari` をもう1つ持たずに済む ――
2つ持つと「サファリなのに段階が無い」状態が表現できてしまう。

### 検証 #109〜#114

| # | 見ているもの |
| --- | --- |
| 109 | `rules:` が指す規則が実在する |
| 110 | **戦えない規則の場所にトレーナーを置かない**（視線に入った瞬間に詰む） |
| 111 | 追い出すイベントに **warp がある**（歩数が尽きても外へ出られない、を防ぐ） |
| 112 | 投げられる唯一のボールが世界のどこかで手に入る |
| 113 | **同じ規則のマップが一続きである**（区画が2つの島に割れていない） |
| 114 | **条件つきの関門が、2つ以上の行き先を同時に塞いでいない**（塞ぐつもりのない道の巻き添え） |

**サファリは「入ったら出られる」ことが全て。** 出口が壊れていると詰みになり、
しかもセーブは中に居る状態で残るので、遊ぶ側からは「詰んだ」ではなく
「壊れた」に見える ―― だから #111 を先に置いた。

#114 はサファリの外で見つかった。ハナダのどうくつの けいび
（`champion-beaten=false` のあいだ居る）が立つマスは、どうくつの入口の
唯一の足場であると同時に、**ショップの扉の唯一の足場**でもあった ――
殿堂入りするまで店に入れない世界になっていて、台本は「品ぞろえが空」としか言わなかった。

#86 は「無条件で warp を塞いでいないか」、#108 は「条件が満たせるか」を見る。
**どちらも塞ぐつもりのところしか見ていない。** #114 が見るのはその巻き添えで、
**1つなら「そこを塞ぐつもり」**（関門として正しい）、**2つ目からは必ず巻き添え**
―― 1人の門番が2つの行き先を同時に閉じる設計は無い。

#113 は上の「区画で数える」を成り立たせるための検査。区画が割れていると、
島から島へ移るのに一度**外へ出る**ことになり、出入りでまた満タンに戻る ――
**数え方は直したのに、制限がまた効かなくなる。**
台本も同じところを見ている: 中央を歩いてから西へ移り、**そこで尽きる**まで回す ――
西だけでは足りない歩数（上限22往復＝440歩）にしてあるので、
**エリアを跨いだ時点で数え直していたら上限に当たって落ちる。**

## 9.16 化石・道場・イーブイ ―― 扉だけが繋がっていなかった（v1.1-g-3-1）

ロケット団の筋に依存しない範囲で、図鑑の穴を先に塞いだ。**ゲームのコードは0行。**

| | 種 |
| --- | --- |
| 化石（かせき2種＋ひみつのコハク） | オムナイト・オムスター・カブト・カブトプス・プテラ = **5** |
| カラテどうじょう | サワムラー・エビワラー = **2**（勝つと どちらか1匹） |
| タマムシマンション | イーブイ＋石で分かれる3種 = **4**（石は v1.1-b で既に世界にある） |

入手経路のある種は **112 → 123**。

### 建物も看板も案内人も居て、扉だけが繋がっていなかった

ニビの博物館とタマムシマンションは、建物・看板・案内人が **v0.12 から居た。**
扉のタイル（`D`）まで描いてあって、**warp だけが無かった** ――
近づいても何も起きない扉が2つ、気づかれずに立っていた。

カントー111枚を数えたら**その2枚だけ**で、どちらも今回作る建物だった。
書き割りが先に建っていて、中身を作る版が来るまで誰も扉を押さなかった。

検証 **#115**（扉として描かれるタイルには warp がある）。#86 は「warp を塞いでいないか」を
見るが、これはその**手前** ―― 絵として扉なのに、そもそも出入口として登録されていない。

> **「機構が無いまま形だけ作らない」の裏返し。形だけ作って、機構を忘れた。**

### かせきは復元してもバッグに残る

`takeItem` は**足さなかった**。1つの版で「使い切る道具」のためにコマンドを増やすと、
`consumable` の意味が道具側とイベント側の2箇所に分かれる。止めるのはフラグにした。

### 推測の仕掛けを2段重ねにするより、実測を1つ

v1.1-h で `goToMap` に「ぶつかったマスを壁として引き直す」を足した。
これが g-3-1 で**セキチクからニビへ戻る道**を通した瞬間に壊れた ――
1回の実行で248回「壁の仮定を捨てる」を繰り返し、
チャンピオンロードの1階から2階へ40回ためして上がれなくなった。

原因は、条件つきオブジェクトを**種類で**扱おうとしたこと:

| 扱い | 壊れるもの |
| --- | --- |
| 一律「素通りできる」 | 当分どかない門番（ハナダのどうくつの けいび）に永久にぶつかる |
| 一律「壁」 | 話しかければ消えるもの（トレーナー・かいりき の岩）の向こうへ行けない |

> **見るべきは種類ではなく、条件が今どうか。**

`draw()` が立っているフラグを canvas に出し（画面には出ない印）、台本はそれを読む。
トレーナーと障害物は素通りできる扱いのまま ―― あれはぶつかれば消せるので
`goToMap` が話しかけて片付ける。**どかない門番だけを避ける。**

v1.1-h で足した3つ（黙って諦める・`walls`・無条件 `accept`）は、
**全部差し戻しになった。** 残ったのは「たどりつけなかったと言う」だけ。

### 「検査が緑」と「機構が働いた」は別（4件目）

視線の区間が落ちたとき、すぐ隣の行は緑だった:

```js
async function fight() {
  if (await page.isHidden("#battle")) return "決着してマップに戻った";
```

**バトルが無ければ即「決着した」と答える。** 視線が1回も働かない回でも、
「視線バトルの 決着」は緑だった ―― 同じ事実を、隣り合う2行が別々に報告していた。

v1.1-h から数えて4件目の同型（`0 == 0` で通るボール・押した物と記録が食い違う検査・
草を1マスも踏まない見張り・これ）。**通っている検査は、通っているという事実しか証明しない。**

### 「視線が壊れた」と「もう倒していた」は同じ字面で出る

視線が働かない原因を追うのに4周（1時間）かかった。画面に出るのはどちらも
「なにも起きない」で、**倒したかどうかをセーブから読む1行**を足した瞬間に決着した ――
「もう 倒している」。`walls` がぶつかり回っている間に倒していただけで、
**機構は最初から正常だった。**

> **分けられないものを見比べても、何周しても分かれない。**
> **測る対象を1つ増やすほうが早い。**

## 9.17 ロケット団の筋（v1.1-g-3-2）

ゲームコーナー → アジト → シルフスコープ → ポケモンタワー2〜7階 → シルフカンパニー。
ライバル戦を **3回 → 7回**。入手経路のある種は **123 → 130**。

**v0.12 でタワーの階を送ったとき「足りないのはシルフスコープの入手元だけ」と
書いた宿題が、ここで解けた。** 先に階だけ作っていたら、登れない7階が1年ぶん立っていて、
ニビの博物館の扉（#115）と同じ形になっていた。

### 入れなかったもの（理由つき）

| | 理由 |
| --- | --- |
| **スロット** | `economy.md` §7 は通貨を お金 と BP の2つに限っている。コインを足すと値段表・交換所・セーブ欄が**全部3系統**になる。原作でコインが担っていたのは「景品を買う」ことなので、景品は**お金で買う**（v1.1-h の `money` 条件がそのまま効いた）。遊べない台を黙って置かず、看板で「しめとる」と言う |
| **回転床** | 氷の床と同じ機構。v1.1-f で「カントーに氷が1枚も無いのでジョウトへ送る」と決めた ―― **ここだけのために先に作ると、その決定が意味を失う** |
| **シルフ 2・4・6・8〜10階** | 同じ事務所の繰り返しで、新しい機構が1つも増えない。増えるのは枚数だけ |

### 鍵は、関門へ向かう道の上に置かない

「カギが無いと地下4階へ行けない」が**行けてしまって**落ちた。
エレベーターのカギを、着地マスから階段への**通り道そのもの**に置いていたので、
確かめに行く途中で拾ってしまう。

> **「そこしか通れない」で関門になるのと同じで、
> 鍵は「寄り道して取るもの」でないと鍵にならない。**

### 検証 #115〜#117

| # | 見ているもの |
| --- | --- |
| 115 | 扉として描かれるタイルには warp がある |
| 116 | 話しかけられる相手には、隣に立てるマスがある |
| 117 | **入ってきた場所から、そのマップの全ての出口へ行ける** |

#117 はアジトの地下1階で見つかった。**上から降りると上り階段のある広間、
下から上がると3マスの袋小路** ―― 袋小路から出るには踏む warp を通るしかなく、
踏めばまた下の階へ戻る。既存の到達性検査は「**どこかの**入口から届くか」しか
見ないので、2つの入口が別々の一角を照らして全マスを埋め、通過していた。

**書き方を2回まちがえた。** 最初は「1枚のマップが1つながりか」で書いたら、
段差・海・テレポート床・扉のマスを例外として数え直すはめになり、
19・20・21番水道とヤマブキジムという**正しい設計を4回落とした**。
そのうえ本命のアジトでは火を噴かなかった ―― 袋小路は
「踏む warp を通れば繋がっている」ので、島としては1つだったから。

> **見るべきは島の数ではなく、壊れ方そのもの。**

### 立つマスと向きは、地図から出す

同じ間違いをこの版だけで3回踏んだ ―― かせきの真上（岩）・景品の店員の真下
（スロット台）・ポスターの真下（階段）。どれも「隣に立って向く」という同じ動作なのに、
座標だけが毎回ちがう。`talkToObject(map, objectId)` を足して、
**立てる隣を地図から選ばせた。** 立てる隣が1つも無ければ「話しかけられなかった」と言う ――
それは台本の間違いではなく世界の間違いで、#116 が拾う側。

## 9.18 交換・小物・名前（v1.1-i）

3つ足した。**どれも「片側だけ作らない」という同じ形の話**になった。

- **NPC交換**（カモネギ・バリヤード・ルージュラ・ベロリンガ）。
  `EventCommand` に `takePokemon` を1件。`givePokemon` だけでは
  「見せると1匹くれる人」しか書けず、**渡した個体が本当に居なくなる**ことに
  交換の意味がある ―― 片側だけなら、それは交換ではなく配布
- **小物3つ**（ディグダのあな・へんしん・テレポート）。
  `fetch-encounters.ts` の `NOT_YET`（野生に出さない種と理由の表）が
  そのまま受け入れ条件になっていた ―― へんしん と テレポート を実装したら表が空になり、
  メタモンとケーシィが野生に出せるようになった
- **版の対**。出現表を FRLG の**両方**（10・11）の和で取るようにした。
  片方だけ読んでいたので、サンド系・マダツボミ系・ヤドン系・ヒトデマン系・カイロスが
  「カントーに居ない種」になっていた。**本作に版の対はない**ので、和を取るのが正しい

### `EventEffect` の書き忘れを、型が黙って通していた

`gavePokemon` を足したとき `field.ts` の `switch` に `case` を書き忘れても
TypeScript は何も言わなかった ―― **戻り値が `void` なので、どの `case` にも
当たらずに抜けるのが型として正しい。** そのままなら「交換したのに手持ちが減らない」。
コマンド側にあった網羅性の番人（`assertAllEventCommandsHandled`）を効果側にも置いた。

### 名前だけが、突き合わせる相手を持っていなかった

`abra` の日本語名を「アブラ」と書いた。追ってみると、間違いは1件ではなく**枠組み**だった。

- `gotItem` / `gotPokemon` は `gameData` から名前を引いて告知するのに、
  イベント側にも同じ告知を**手で46行**書いていた（v0.7 から）。そのうち4件が誤り
- 直し方は「4件を直す」ではなく、**手で書く場所を無くすこと** ――
  46行を消し、検証 #118 が「入手の告知をイベントに書かない」を見張る

v1.1-j でこの掃除の続きをやった。`tools/species-ja.tsv` と `tools/abilities-ja.tsv`
（人が書いた日本語名の表）を捨て、veekun の `ja-Hrkt` から引く。
**種族名は v0.9.5 の時点で守られていた**（`import.ts` が `species-numbers.tsv` と
突き合わせる）が、**特性名はどこも見ていなかった** ―― そこに検証 #119 を置いた。

## 9.19 ナナシマ ―― 完了条件は地図の外にあった（v1.1-j）

v1.1 の完了条件は「カントー151種すべてに入手経路がある」で、147種で止まっていた。
残り4種のうち3種は**ポニータ・ギャロップ・ブーバー**。
どれも赤緑ではカントー本土に居たが、**FRLG でナナシマへ移された**種で、
ナナシマを作らないかぎり、カントーをどれだけ埋めても届かない。

> **完了条件が閉じない理由は、閉じない側の地図の中には無かった。**

作ったのは 1〜3のしま（26枚）。入手経路 **147 → 150 / 151**。
残る ミュウ は配布限定なので、穴ではなく**理由の書ける空欄**。

### 出現表は1行も書いていない

`fetch-encounters.ts` の `SOURCE` に14行足して回しただけで、
53エリアぶんの公式データから 115表が出てきた（表 78 → 193・野生に出る種 49 → 97）。
v1.1-a に「マップを1枚足す → 取り込みを回す → 行を貼る」と書いた形が、
**26枚まとめてでも同じコストで効く**ことを確かめた。

**山の外は `area: ""` で引く。** 省略すると `mt-ember` の全エリア
（洞窟・地下1〜3階）が1つの表に混ざり、山道を歩いているのに マグカルゴ が出る ――
クチバの港（`ss-anne-dock` が町の海に混ざる）で踏んだのと同じ形。

### 関門は「置いた」ではなく「そこしか通れない」で初めて関門になる

シーギャロップ号の桟橋を町の水辺に置くと、**なみのり で横から乗り込める** ――
この時点のプレイヤーは グレンじま へ行くために なみのり を持っている。
だから のりばを**建物にした**。屋内なら、タラップの手前に1人立つだけで塞がる。

港どうしは**鎖でつないだ**（クチバ → 1 → 2 → 3）。原作は行き先を選ぶ形だが、
`choice` + `warp` は**地図の繋がりとして見えない** ―― 検証も台本も `warp` しか辿れないので、
「選べるが誰も検算できない道」より「歩いて辿れる道」を採る。

### 検証が5件つかまえた

どれも遊べば必ず踏むもので、**書いた本人には見えない**種類:

| 何を | どう壊れていた |
| --- | --- |
| #47 warp | 1のしまの北の出口が、**ほのおのみちの warp の上**に降ろしていた（無限往復） |
| #117 一方通行 | 上と同じ原因で「入っても出られない道」に見えていた |
| #48 視線 | ぼうそうぞくに条件が無く、**倒したあとも視線で襲ってくる** |
| #116 オブジェクト | 砂浜の隠しアイテムを**水の上**に置いていた |
| #1 参照 | 存在しない道具3件（`escape-rope` / `nugget` / `rare-candy`）を配ろうとしていた |

### 実測（v1.1-j）

| 道具 | 結果 |
| --- | --- |
| `npm run check` | 型・テスト408件・**検証119項目**・生成物の鮮度、すべて緑 |
| `npm run playthrough` | **278件・失敗0件・22.5分**（ナナシマ区間つき） |
| `npm run shots` | **62か所 × 2サイズ・`△` 0件** |

台本のナナシマ区間で確かめているのは、**塞がっている側と開いた側の両方**:
「はじめは まだ しゅっこうできない（乗れない）」→ マサキに話す →「乗れる」→
1のしま → ほのおのみちで野生 → ロケット団2人 → ルビー → セリオ →
港の鎖を3つ辿って 3のしまの みなと。

### 壊れた足取りは、後ろの成功に隠れる

台本のナナシマ区間を初めて回したとき、**280件中の1件だけが落ちた** ――
「ネットワークセンターに 戻れる」。狙った座標 (4,3) が**機械の上**（通行不可）で、
経路探索は「経路なし」と正しく言っていた。

見落としそうになったのは**その次の行**で、
「ルビーを 渡すと マシンが うごく: うごいた」が**通っている**。
`talkToObject` は立てる隣を自分で選び直すので、
**セリオには実際に会えていた** ―― 到着に失敗したのに、用件は果たされていた。

> **後ろの検査が通ったからといって、前の検査の失敗が軽いわけではない。**
> 落ちた1件は「台本が下手」で、通った1件は「世界は正しい」―― 別々のことを言っている。

### 入れていないもの（理由つき）

- **みさきの わざおしえ** ―― 教え技の機構が無い。道具でもないので、
  `fetch-official.ts` が `'T'` を入れないのと同じ理由
- **きのみの木** ―― 持ち物としての きのみ はあるが、木から採る機構が無い
- **`mt-ember/cave`** ―― `inside` と同じ種しか出ず、地形の繋がりにも要らない
- **4〜7のしま** ―― 殿堂入り後の島。氷の床（v1.1-f でジョウトへ送った宿題）と
  非カントー21種が要るので、版を分けた（v1.1-k）

## 9.20 氷の床 ―― 「一歩＝1マス」が崩れる場所（v1.1-k）

v1.1-f で `boulder` を足したとき、氷は**カントーに1枚も無い**として送った。
こおりのぬけみち（4のしま）にある ―― 宿題は地方をまたいで戻ってきた。

### 滑走は1回の呼び出しで全部返す

`core` は時間を持たない。「1マスずつ返して UI が繰り返す」形にすると、
**途中で止められる世界**になってしまう ―― 止まれるなら、それは滑走ではない。
`slideFrom` が通ったマスの並びを返し、`stepPlayer` は
**止まる場所を決めてから**いつもの判定（出入口・道具・視線・野生）をそこで行う。

**`slid` は `StepOutcome` ではなく `StepPlayerResult` に置いた。**
滑り終えた先で起きることは、滑ったかどうかと無関係だから ――
結果の種類にすると「滑って warp に入った」を表すのに種類の掛け算が要る。

> **滑走は移動の仕方であって、結果ではない。**

### 教えた場所は2つだけ

`stepPlayer`（実際の1歩）と `neighborsOf`（行き先の集合）。
v1.1-a で「隣とは何か」を `core` に一本化してあったので、
**検証も台本2本も、氷を知らないまま正しい道を引く** ―― 一本化の利息をここで受け取った。

### 検証 #55 が、正しい氷の部屋を落とした

氷は**通るマスであって立てるマスではない**ので、帯の途中が全部
「到達できないマス」になる（v1.1-f で #55 が障害物を壁と数えて誤報したのと同じ形）。
塗りつぶしに「滑って通り過ぎたマスも数える」を足した ――
逆に、**どの滑走も通らない氷は本当に閉じている**ので孤立として残り、
実際に3マス見つけて地形を直した。

### 検証 #120 ―― 止まらないと触れないものを、氷の上に置かない

滑走中は止まれないので、氷に置いた道具は拾えず、NPC には話しかけられない。
**データとしては正しく、遊ぶと存在しないのと同じ**になる。
踏む warp のほうは壊れ方が派手で、止まる前に画面が切り替わる。
両方わざと置いて火を噴かせてから入れた。

## 9.21 入れられなかったもの ―― アンノーンで詰まったのは技だった（v1.1-k）

アンノーンはフォルムを28持つ種で、**フォルムの機構が無いのは承知のうえ**で
「1文字ぶんの1種」として入れようとした。詰まったのはそこではない。

レベルで覚えるのは **めざめるパワー だけ**で、あの技は
**個体ごとにタイプが変わる**。デリバードの プレゼント は
「威力を実行時に決める」で足りた（`damage.ts` の `powerOverride` は v0.4 から
開いていて、使う者が誰も居なかった口 ―― その最初の通行人になった）が、
アンノーンは**型が動く**。個体でタイプが決まる仕組みが無いまま置くと、
技を1つも使えない野生になる ―― 検証がそう言って止めた。

> **同じ「実行時に決まる」でも、値が動くのと型が動くのでは重さが違う。**

だから遺跡は「とびらが かたく とじている」まま置いた。
原作でも タノビーのカギ を見つけるまで封印されているので、
**閉じている状態そのものが原作準拠**になる ―― 開かない扉を作ったのではない。

## 9.22 台本の脆さは、世界が広がるだけで増える（v1.1-k）

4〜7のしま を足したあと台本が**30件落ちた**。数えてみると、本物の失敗は**2つ**で、
残る28件はその連鎖だった ―― マサキが誘わない（シルフを解けていない）から先が全部倒れた。
**連鎖のほうが目立つので、まず「何件落ちたか」ではなく「どこで最初に落ちたか」を見る。**

### 遠くを歩かせる区間は、書いた日より遠くなる

ニビの博物館へ、カビゴンを起こした場所から歩かせていた。
途中の おつきみやま で野生に何度も割り込まれ、120回の引き直しを使い切って届かない。
**台本は1文字も変えていない。** 変わったのは世界の広さ（130 → 195枚）だけ。

> **移動する区間は、世界が広がるほど勝手に脆くなる。**
> 近くまで そらをとぶ で飛んでから歩く ―― 台本が測りたいのは「扉が繋がっているか」であって、
> 「そこまで歩き切れるか」ではない。

### わざと失敗させる検査は、失敗したあとの立ち位置を決めない

「カードキーが無いと7階へ行けない」は、6回ぶん歩き回ってから諦める検査。
**諦めた場所は毎回ちがう。** 実際 (5,2) に取り残されていて、そこは団員が塞ぐ側だった ――
次のカードキー拾いが20回引き直しても届かない。

失敗を確かめる検査のあとは、**立ち位置を決め直してから**次へ進む。

### 落ちたのが台本かゲームかを、推測ではなく実測で分ける

「氷を足したせいでは」「種を35足したせいでは」と当たりを付けかけたが、
**シルフ5階にプレイヤーを置いて実際に歩かせる20行の検査**を書いたら、
ぶつかる → z → 会話 → バトル開始 まで正しく起きた。ゲームは無実だった。
撮影（`shots`）が同じ場所を撮れていたことも、同じ方向の証拠になる。

> **道具が落ちたときに真っ先に疑うべきは、直前に触った機構ではなく「道具の前提」。**

## 9.23 密度 ―― 足りなかったのは仕掛けでも種でもなく、人の数だった（v1.1-i）

地理は原作どおりになり、種は 228 になり、仕掛けも揃った。
それでも歩くと**すかすか**だった。数えたら、道路25本にトレーナーが合わせて13人しか居ない
（原作は1本あたり4〜8人）。ジムは8つとも、リーダーの他に**ちょうど1人**しか居なかった
（原作は2〜6人）。いまは 17人 足して 8 → 25人。

### 手で足すと1人あたり5箇所

`trainers.tsv` / `trainer-parties.tsv` / `flags.json` / `events.json` に2件 / `.map` に1行。
**79人ぶんを見比べたら、37人が「話しかける → 勝つまで戦う → 勝ったら消える」の
まったく同じ形**で、変わるのは台詞2つだけだった。

そこで `trainers.tsv` に `before` / `after` の2列を足し、
**残りは `import.ts` が組み立てる**（`events-trainers.json`）。
落ちている道具も同じで、`.map` の1行が全部言っているので
`convert-map.ts` が `events-items.json` を組み立てる。

| | 前 | 後 |
| --- | --- | --- |
| トレーナー1人 | 5箇所 | **3箇所**（定義・手持ち・地図の1行） |
| 落ちている道具1つ | 3箇所 | **1箇所**（地図の1行） |

### 書き写していたから、ずれていた

移す途中で、**組み立てた結果と手書きが食い違うイベントが15件**出た。
どれも組み立ての誤りではなく、手書き側のずれだった:

- 15件の出どころは**6人**で、全員 **名乗りが `class` と違っていた**
  （ロケットだん／したっぱ、ジムトレーナー／かえんむすめ）。
  名乗りの正解を1つ選ぶ必要があり、`class` を採った ―― 表の列がそう言っている
- 別口でもう1件。1番道路の道具は、
  **地図に キズぐすり と書いてあるのに オボンのみ を渡していた**

どちらも「同じことを2箇所に書いて、片方だけ直した」跡。組み立てにすれば、ずれようが無い。

### 置く場所は、人ではなく道具が選ぶ

`tools/add-trainers.ts` / `add-items.ts`。座標を人が決めると通路を塞ぐ
（v1.1-k でシルフ7階が実際にそうなった）。判定は2つだけ:

1. 置いても**今より切り離されたマスが増えない**か
2. 置いても**他の NPC の最後の空き隣を奪わない**か

1つめは最初「全部つながっているか」で書いて、**既に誰かが割っているマップで候補0**になった
―― 見るべきは全体の完全さではなく、**自分が置かれたことで減るかどうか**。
2つめは検証 #116 が火を噴いて分かった（道具がカツラを壁で囲んだ）。
端から順に取ると道の隅に一列に並ぶので、**離れているものから選ぶ**。

### タマムシデパート ―― 品揃えは階で分ける

v1.1-b の時点で「階を分けるのは v1.1-i」と書いてあった宿題。
1階 案内／2階 どうぐ／3階 わざマシン／4階 しんかの どうぐ。

**同じ店員に品目を足していくと、「バッジで増える品揃え」と
「階で違う品揃え」が1つの表に混ざる。** 混ざったものは後から分けられない。

わざマシンは `machines.tsv` から作られるので `items.tsv` に値段の列が無かった。
店に並べるには値段が要る ―― `machines.tsv` に `price` を1列足して取り込みが渡す。
**値段を2か所に書かない。**

### 建物が無くなったら、店員も居ない

原作のタマムシに フレンドリィショップ は無く、デパートがその役をしている。
`kanto-celadon-mart` をデパート1階に作り直した時点で、
バッジで品揃えが変わる店員（`kanto.celadon.clerk`）は**居場所を失った**。

検証の [event-unused] が「誰も呼ばない」と言ったとき、
呼ぶ人を探して繋ぎ直すのは間違いだった ―― **建物ごと無くなっている。**
消して、理由を地図の頭に書いた。仕掛け自体（`kanto-mart-1/2/3` をバッジで選ぶ）は
他の5つの町で今も動いているので、失われた機構は無い。

> **「入れていない」と「入れ忘れ」を、消すときにも区別する。**

### 歩けたときの待ちで、ぶつかったときを待っていた ―― 差は15ms

密度を上げた版で、台本が5番・6番道路のちかつうろ出口から**一歩も動けなくなった**。
60回引き直して諦め、そこから先の区間が全部連鎖で落ちた。

**推測せず、3段で切り分けた。**

1. (9,5) に立たせて手で ↓ → z を押す → 会話もバトルも正しく起きた ―― **ゲームは無実**
2. 台本の `goToMap` をその位置だけ切り出して回す → 同じように詰まった ―― **台本の話**
3. 「↓ のあと何ms で z を押すか」だけを変える → **境目が出た**

```
↓のあと  60ms で z → 会話は開かない
↓のあと 120ms で z → 会話が開く
```

答えはゲーム側にあった。`field.ts` の **`BUMP_MS` は 110ms** ―― 壁や人にぶつかると
それだけ止まる。台本の1歩の待ちは**じてんしゃで 95ms**で、
これは「歩けたときのアニメ」に合わせて決めた数字だった。

> **歩くとぶつかるでは長さが違うのに、片方の数字で両方を待っていた。**

直し方は待ち時間を足すことではない ―― 足せば、アニメを変えた日にまたずれる。
**時間を数えるのをやめて、受け付けているかを読む**:
`field-canvas` に `data-busy` を出し、台本はキーのあとそれが下りるのを待つ。
`data-at`・`data-flags` と同じ「推測せずに読めるようにする」印で、
歩き・ぶつかり・滑走のどれにも同じ待ち方が効く。

**なぜ今まで出なかったか。** ぶつかって話しかける相手が、
じてんしゃに乗ってから通る道に居なかった。
5番・6番道路の出口の真下にトレーナーが立ったのがこの版で初めてで、
15msの差はそこで初めて意味を持った ―― **密度は、時間の噛み合いも試す。**

### 撮影の「倒した扱い」を、並べるのをやめてデータから引いた

`shots.mjs` は視線を持つ相手を避けられない（話しかける仕組みを持たない）ので、
トレーナーを倒した状態にして撮る。その一覧を **v0.12-e から1人ずつ手で並べていて、
45行**まで伸びていた ―― ジムに17人足したところでまた足りなくなった。

トレーナーは全員 `defeatedFlag` を持っている（検証が見ている）。
並べるのをやめて `trainers.json` から引くようにした。
**これも「同じことを2箇所」だった** ―― 増えるたびに片方だけ直す作業が消えた。

## 9.24 道具が2本あるなら、直しも2本に要る（v1.2-a）

たきつぼ に滝の帯を引いた翌日、撮影が**19枚 `△`** を出した。
原因は2つあって、片方を直しても もう片方が残った。

### ① 遠い1枚を列の途中に置くと、その先が全部ずれる

撮影は1枚ごとにセーブを読み直さず、**前の場所から歩いて次へ向かう。**
たきつぼ をカントーの列の途中に置いたら、**次の1枚がタマムシまで161歩**になり、
`goTo` の引き直し60回を使い切った。経路は161歩でちゃんと在る（実測）――
**並び順がそのまま歩く距離になる。**

台本の「確かめる場所の隣に居るうちに確かめる」（§9.22）と同じ話が、撮影にもあった。

### ② 150ms は、滑走にもぶつかりにも足りない

並び順を直して19枚→12枚に減ったが、残りは別の原因だった。
**同じ区間だけを切り出して、`ready()` の有無だけを変えて測った:**

```
ready なし: 届かなかった（いま kanto-sevii-icefall-waterfall 6,8）
ready あり: 着いた（引き直し 1回）
```

150ms は「1歩ぶんの歩行アニメ」に合わせた数字で、
**氷を滑るあいだ**（1マス55ms × 滑った枚数）にも
**ぶつかったとき**（`BUMP_MS` 110ms）にも足りない。

**台本が同じことで落ちたのは、その前日だった**（v1.1-i・`data-busy` を足した回）。
直したのは台本だけで、撮影は150msのまま残っていた ――
**同じ前提を持つ道具が2本あるなら、直しも2本に要る。**

> 気づけたのは、**滑る場所を通る絵を初めて撮った**から。
> 前提が間違っていても、その前提を踏む道が無いあいだは緑のままでいる。

### ③ 出典を原作の版へ移すと、道具の前提のほうが古くなる

互換表を FRLG の版へ移した日、台本のわざマシンの区間が落ちた ――
**ヒトカゲの系統は原作で がんせきふうじ を覚えない。**
第9世代の表では覚えたので、それまで通っていただけだった。
前半4つのジムがくれるマシン（39・03・24・19）は**どれも覚えられない**
―― これも原作どおりで、ヒトカゲは序盤のマシンと噛み合わない。

> **データを原作へ寄せると、原作を知らない道具が落ちる。**
> 落ちたのは正しさの現れで、直すのは道具のほう。

### ④ 台本が「泳げる能力」を1つだけ手で持っていた

たきのぼり を教わったあとも、滝の向こうへ「経路なし」と言った。
`able()` が **`canSurf ? ["surf"] : []`** と書いてあり、
**なみのり以外の「上を歩く能力」を知らなかった。**

v1.1-c で `canSwimTo` の `"water"` と `"surf"` を `core` から追い出して
`field-abilities.json` へ寄せたが、**同じ形の穴が道具側に残っていた** ――
能力が増えるたびに手で足す場所が1つあった。

いまは画面の `data-flags` と `data-badges` を読んで `fieldAbilitiesFor` に
そのまま判断させる。**能力が1つ増えても、台本には何も要らない。**

## 10. 調整項目（実装後に決める）

- タイルサイズ（16px か 32px か）と画面あたりの表示タイル数
- エンカウント率と救済の閾値
- トレーナーの視線の距離
- マップ1枚あたりのオブジェクト数の上限（描画負荷）
- イベントの `wait` の単位（フレームか ミリ秒か）
- マップの遅延読み込み単位（1枚ずつか、隣接分を先読みするか）
