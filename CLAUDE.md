# ポケモン風RPG（ブラウザ版）

`python3 serve.py 8123` で配信して `http://localhost:8123` で遊ぶ。
ビルドステップなし・依存ゼロの ES modules。import は必ず相対パス＋`.js` 拡張子。

## 開発の進めかた

- ブランチ: `claude/pokemon-style-game-s4v9rf`。ここ以外に push しない。
- `first.txt` は git 練習用の既存ファイル。触らない。
- 画像・音声の著作物アセットは使わない。ドット絵はすべて `src/data/sprites/` の
  ピクセル文字列から自作する。
- テスト: `tests.html` をブラウザで開くか、Playwright で叩いて `ALL PASS` を確認する。
- 仕上げ: `node tools/bundle.mjs` → `--fragment` → `node tools/datasheet.mjs` で
  `dist/` を再生成し、アーティファクトを再公開する。

## 長期方針 — 少しずつ育てる

**最終目標: ポケモンを全部入れる。あわせて歴代のジムリーダーと町を登場させる。**

ただし **1回の実装で全部やらない**。毎回の作業に「ちょっとずつ」混ぜていく。
1回あたりの目安:

- 新種族 **3〜6体**（系統単位で。行き止まりの1体だけ足すより進化ラインで足す）
- 町またはジムを **0〜1つ**
- その回に必要なわざ・とくせいがあれば一緒に

新機能や改善の依頼を受けたときも、可能なら上の増分を1つ相乗りさせる。

### 世界の現状（カントーをなぞっている）

```
はじまりのむら → １ばんどうろ → トキワシティ[1 マチス/でんき]
  → トキワのもり → ２ばんどうろ → ニビシティ[2 イワオ/いわ]
  → おつきみやま → ハナダシティ[3 カスミ/みず]
  → ハナカゴシティ[4 リンドウ/くさ] → ボレイどうくつ[5 コトリ/ゴースト]
  → ３ばんどうろ → ポケモンリーグ（エリートフォー3人＋チャンピオン）
```

地名は本家カントーを踏襲しているが、リーダーは一部が独自名・独自配置。
`イワオ`（ニビ・いわ）は本家の `タケシ` と同じ町・同じタイプの代役。
`マチス` は本家ではクチバのリーダーで、トキワは本来 `サカキ`（じめん）。

### 町とジムの追加順（案）

| # | 追加するもの | タイプ | 備考 |
|---|---|---|---|
| ✅ | ハナダシティ ／ カスミ | みず | おつきみやまの直後。本家どおりの位置 |
| 次 | クチバシティ ／ マチスを移設 | でんき | トキワジムが空くので サカキ/じめん に |
| | シオンタウン ／ ポケモンタワー | ゴースト | ボレイどうくつの役割を吸収するか併存させる |
| | タマムシシティ ／ エリカ | くさ | ハナカゴジムと同上 |
| | セキチクシティ ／ キョウ | どく | |
| | グレンじま ／ カツラ | ほのお | なみのりで渡る |
| | ヤマブキシティ ／ ナツメ | エスパー | |
| | トキワジム ／ サカキ | じめん | カントー完結。ここまで来たらジョウトへ |

バッジ数は `BADGES` 配列から導出されるので、増やすときは
`src/data/trainers.js` の `BADGES` に足せば `allBadges` 判定は自動で追従する。
`achievements.js` の実績名と `tests.html` のバッジ数アサーションだけ手で直す。

### 種族を足すときの穴（優先して埋めたい）

進化ラインが1本しかないタイプ ＝ プレイヤーに選択肢がない。

- **いわ** … イシツブテ系のみ
- **ドラゴン / あく** … どちらもモノズ系のみ（同一系統）
- **エスパー / フェアリー** … どちらもラルトス系のみ（同一系統）
  → スターミー追加で エスパー は2系統になった

わざのタイプ偏りも同じ観点で見る（ノーマル15本に対し ドラゴン2本 など）。

## 設計上の約束

- **依存の向き**: `data/*` は何も import しない純データ → `game/*` は `data/*` と
  `core/*` のみ → `scenes/*` は何でも可。
- **バトルはジェネレータ**。`battle.js` が「エフェクト」を yield し、
  `BattleScene` が実行して `next()` で再開させる。
- **導出値はセーブしない**。`maxHP` などはロード時に再計算するので、
  計算式の修正が既存セーブにも自動で効く。
- `effect.kind` の未対応値は「素の攻撃技」として素通りする。
  データがエンジンより先行してもクラッシュしない。
- 基準解像度 **256×192**、整数倍スケールのみ、`imageSmoothingEnabled = false`。
